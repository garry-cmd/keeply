# Keeply — Context

**Updated:** April 28, 2026 (late evening — coverage CTA pass + AuthOpenerProvider lift + branded password-reset & verification emails; 7 patches shipped to main)
**Phase:** GoLive Ready · Coverage-first messaging done across site + metadata · AuthModal lifted to layout-level provider (no nav-flash on auth)
**Founder:** Garry Hoffman (solo)
**Target:** $5K MRR to quit day job

---

## Strategic frame

Keeply is a **vessel intelligence platform**, not a maintenance app.
The app UI is a read-only dashboard; **First Mate is the primary interaction layer.**
Copy rule: never use "sailors" — always "boaters."

**Apr 26 contest note (still active):** the website + metadata now lead with COVERAGE ("Every system. Every part. Every passage.") rather than First Mate. The "vessel intelligence platform" framing is internal-only as of Apr 28; user-facing surfaces are coverage-first. First Mate is a differentiating feature, not the headline product positioning. See Operating rules → Content for details.

---

# OPERATING RULES — READ FIRST EVERY SESSION

> Everything below this section header through "Reference data" is the procedural surface area for working on Keeply. Future Claude reads this part first; the body of the doc (Current state, What's shipped, Active work, Key learnings) is reference for context-aware decisions, not boot-up.

## Session-start ritual

CONTEXT.md and ROADMAP.md live in git as the single source of truth. They are **not** kept in Claude project knowledge — project knowledge is reserved for reference material that doesn't change (ICP, marketing plan, logo). Session-start fetches the live docs directly from the repo.

1. `recent_chats` — last 3–5 threads
2. Clone the repo: `git clone --depth 1 https://github.com/garry-cmd/keeply.git /home/claude/keeply` (~4 sec, gets whole codebase ready for grep/edit as a side effect)
3. Read `/home/claude/keeply/CONTEXT.md` and `/home/claude/keeply/ROADMAP.md`
4. `tool_search` to load Chrome MCP tools, then navigate to `https://keeply.boats/admin/okr` for live OKR state (auth-gated on `garry@keeply.boats`; Chrome extension must be active in browser)

**If the clone fails with auth error:** repo is private. Ask Garry to flip it public (his standard pattern) or supply a read-only GitHub PAT in-chat. Don't silently fall back to project knowledge — that defeats the single-source-of-truth rule.

**If filesystem tools (`create_file`, `bash_tool`, `view`, `str_replace`, `present_files`) are unavailable mid-session:** flag this immediately and stop the deploy flow. Do NOT work around the limitation by pasting code in chat — that breaks the standard delivery contract and forces Garry to assemble files manually. Either wait for tools to come back or pause the session.

## Deploy workflow — never skip

1. **Default: single push to main.** `git push origin HEAD:main` after `npm run build` succeeds locally.
2. **Staging-first ONLY for** schema migrations to prod, payment flow (Stripe/checkout), auth flow (signup/login/verify), or anything writing irreversible production data. Sequence: push to staging → test at `keeply-git-staging-garry-cmds-projects.vercel.app` → if clean, push to main as a separate step.
3. **Mental test:** "if this breaks, can I push a fix in 60s and forget it?" Yes → main. No → staging first.
4. **Rollback:** Vercel → Deployments → READY deploy → Promote to Production.
5. **Always end coding sessions with PowerShell commands.** Single fenced block, copy-paste order, no inline checklists/confirm prompts/commentary between commands. **File delivery contract:**
   - Stage all changed files in `/mnt/user-data/outputs/keeply-<change-name>/`.
   - Use `present_files` to surface them — files arrive in Garry's `C:\Users\garry\Downloads\` flat (no nested folders).
   - Use **explicit per-file** `Copy-Item -Force C:\Users\garry\Downloads\<File> <repo-path>` for each changed file. Never `$env` variables. Never directory-level `Copy-Item -Recurse`. Never paste code in chat as a substitute.
   - Default ends with `git push origin HEAD:main`. For staging-first changes, end first block with staging push, brief "test, then run:" note, then a separate block for the main push.
6. **Code and docs deploy together.** Any PowerShell deploy bundle that ships code must either include matching CONTEXT.md / OKR page updates in the same commit, or Claude must explicitly state "no context changes needed" at the top of the deploy instructions.
7. **Docs sweep is ONCE at end of session, not per commit.** For sessions with many incremental commits, batch CONTEXT.md / `app/admin/okr/page.tsx` updates into a single docs commit at the end — reduces noise and avoids regenerating patches as changes evolve. Mid-session doc edits are a violation.
8. **End of session is determined by Garry.** Never preemptively offer to wrap up, write closing summaries, or update CONTEXT.md / OKR page unless Garry explicitly says the session is ending. Treat each turn as mid-session work until told otherwise. Never ship code in one push and reconcile docs in the next session — that is exactly how "Database-driven pricing" made it into CONTEXT.md for months while the code did something else entirely.
9. **History note (Apr 26):** Old policy was "always staging then prod, never main directly." Reversed because 14+ same-day deploys never tested the staging URL between pushes (it was paying ceremony) AND a Features CTA fix was lost when the second push step never ran. New default reflects what was actually safe in practice.

## Coding

- Discuss architecture and show mockups before writing code
- Flag honest tradeoffs; don't just build what's asked
- Batch non-critical bugs; fix blockers immediately
- **Do NOT split `KeeplyApp.jsx` (~26,000 lines post-Prettier, ~8k substantive) pre-launch** — post-launch tech debt
- When applying multiple patches, always work from the most recent file state
- TypeScript errors in the container are pre-existing env issues, not regressions
- **Verify handoff specs against live DB / live code before executing them.** Specs written in one session and executed in another can silently drift out of sync with reality. Preflight (grep counts + DB state check) takes a minute and prevents catastrophic edits.

## Content

- Never "sailors" → always "boaters"
- ~~Never reposition as "maintenance app" — vessel intelligence~~ — **CONTESTED as of Apr 26.** About page scrub demoted "vessel intelligence platform" → "where your boat's whole record lives" and demoted First Mate from "primary interaction layer" to "help when you want it." Hero copy now leads with coverage ("Every system. Every part. Every passage."), not AI. Apr 28 evening session flipped all root metadata in `app/layout.tsx` (title, description, openGraph, twitter) to coverage-first, so the messaging conflict is now resolved across visible surfaces — but the strategic-frame statement at the top of this doc stays as the internal positioning. Use coverage-led copy on every website surface; only invoke "vessel intelligence platform" framing in internal/strategic discussions.
- ~~First Mate is the product~~ — **CONTESTED, see above.** First Mate is a differentiating feature, but not the headline product positioning anymore. The product is the connected record; First Mate is how you talk to it.

## Admin

- Admin email: `garry@keeply.boats` (not svirene.com)
- Additional admins via UUID whitelist in Vercel env var
- Personal email: `garry@svirene.com`

## Connector access

Claude's connectors split into two layers — knowing which is which prevents wasted setup time.

**Direct (callable in chat):**
- **Gmail** — threads, search, drafts, labels
- **Supabase** — SQL, logs, migrations against `waapqyshmqaaamiiitso`
- **Vercel** — `list_deployments`, `get_deployment_build_logs`, `get_runtime_logs`
- **QuickBooks** — full accounting
- **Claude in Chrome** — live site review, DOM inspection, localStorage reads, network/console monitoring. Requires the Chrome extension active in Garry's browser.

**Artifact-only (MCP server, only reachable from AI-powered artifacts):**
- **Google Calendar**
- **Google Drive**
- **Stripe** — for Stripe data in chat, workarounds: query Supabase for subscription records, check Vercel logs for webhook activity, or build a small Stripe artifact on demand.

**All deferred tools require `tool_search` first.** Chrome, Gmail, Vercel, Supabase, QuickBooks tools are NOT in the default toolset — Claude must call `tool_search` with a relevant query to load them before first use in each session. Easy to forget. If Claude says "I don't have X access," check this before re-running connector setup.

### Other tooling notes

- **Supabase auth token key:** `sb-waapqyshmqaaamiiitso-auth-token`
- `raw.githubusercontent.com` and `api.github.com` are both blocked in Claude's network allowlist. Only `github.com` itself is reachable, which means `git clone --depth 1 https://github.com/garry-cmd/keeply.git` is the canonical way to read repo contents. Don't attempt API or raw-content URLs — they'll 403.
- User deletion in Supabase: `DELETE FROM auth.users` cascades to all child tables
- Auth diagnostics via Supabase `get_logs` with `service: "auth"`

## Git gotcha

Running git commands from `C:\Users\garry` instead of `C:\Users\garry\keeply` produces fatal "not a git repository" errors. Catch early.

---

# REFERENCE DATA

## Stack

- **Frontend:** Next.js/React, monolithic `components/KeeplyApp.jsx` (~26,000 lines post-Prettier, ~8k substantive)
- **Marketing:** `components/marketing/LandingPage.tsx` (148 lines) + 8 section components in `components/marketing/sections/` + `components/marketing/PhoneScreenshot.tsx`. Pure presentation, zero Supabase imports on the marketing path.
- **Auth UI:** `components/auth/AuthModal.tsx`, `useAuthRedirects.ts` URL-param hook, `AuthOpenerProvider.tsx` (layout-level provider; lifts modal mounting above HomeClient so off-homepage CTAs don't nav-flash). PlanPickerModal still in repo but orphaned as of Apr 28.
- **DB/Auth:** Supabase — project `waapqyshmqaaamiiitso`. PKCE flow type (delivers password-recovery as `?code=` query param, NOT `#access_token` hash; `PASSWORD_RECOVERY` event does NOT fire reliably under PKCE — use custom `?keeply_recovery=1` URL marker).
- **Hosting:** Vercel — project `prj_494TKw1owvgGjSW8UNFB4oplrBMU`, team `team_FD2H6R0bDq59mIOZWsPE8YLg`. Build Machine on Standard tier (was Turbo until Apr 27 — fixed $120/mo bill).
- **AI:** Anthropic API — Sonnet for quality, Haiku for volume
- **Payments:** Stripe (live keys on prod AND staging — long-term swap staging to `sk_test_`)
- **Email:** Resend (sender `Keeply <noreply@keeply.boats>`)
- **Local path:** `C:\Users\garry\keeply`
- **Repo:** `github.com/garry-cmd/keeply` (toggles public/private)

## Pricing (Apr 25 2026)

| Tier | Monthly | Annual | Limits |
|---|---|---|---|
| Free | $0 | — | 1 vessel, 2 equipment, 3 repairs, 5 First Mate queries |
| Standard | $15 | $144 | unlimited equipment, 30 AI queries |
| Pro | $25 | $240 | + voice + weather + departure checks |
| Fleet | $49.99+ | — | multi-vessel |

## Stripe price IDs

- Standard Monthly: `price_1TKJ3GA726uGRX5eqmN6Rwr4`
- Standard Annual:  `price_1TKJ3GA726uGRX5eroj4WEUp`
- Pro Monthly:      `price_1TKJ3TA726uGRX5epzWsSkbN`
- Pro Annual:       `price_1TKJ3kA726uGRX5eRna7Gr4P`

---

# CURRENT STATE

## Recent sessions (most recent first)

- **Apr 29 session — multi-engine tracking shipped (Phases 2A–2G) + Lists feature rebuild + admin Lists metrics. Two major workstreams in one session.** **(1) MULTI-ENGINE.** Strategic decision up front: 70% of future customers are multi-engine and beta tester pushback on missing support was loud — full deprecation of `vessels.engine_hours` is the destination, this session takes 6 of 7 phases there. **Phase 1 schema:** `passage_engine_hours` table created with member-allowed delete RLS; `equipment.engine_id` FK column added (`ON DELETE SET NULL`); backfill SQL linked test vessel "MV Multi Test" port/starboard engine equipment cards to engine rows. **Phase 2A — foundation + reads:** new `lib/engines.js` module is now single source of truth for engine helpers — `getOrderedEngines`, `getMaxHours`, `hasEngineDiscrepancy` (>50hr abs OR >10% pct), position labels, `getEngineHoursForTask`, etc. `KeeplyApp.jsx` got engines state + `loadEnginesForVessel` + KPI strip rewrite (single/twin/triple+ variants with discrepancy pulse), 4 `vessels.engine_hours` mirror sites hooked to also write engines table. `FirstMate.jsx`, `FirstMateScreen.jsx`, `app/api/firstmate/route.js`: per-engine context entries replace single `engineHours/engineHoursDate` fields; FM now reasons over `vessel.engines: getEngineContextEntries(engines)` array. **Phase 2C — multi-engine update modal:** per-engine input sections with current state + discrepancy preview, parallel saves via `Promise.all`. **Phase 2D — passage form:** `LogbookPage.jsx` accepts engines prop, single-engine layout unchanged, multi-engine gets per-engine labeled sections; `savePassage` uses DELETE-then-INSERT on edits + mirrors first engine to `vessels.engine_hours`. **Phase 2E — `VesselSetup.jsx` unlimited engines:** position canonicalization (1=null, 2=port+starboard, 3=port+center+starboard, 4+=engine_1...N), `reassignPositions` helper, inline long-form labels. **Phase 2F — equipment ↔ engine linking:** `getEngineHoursForTask` wired into all 8 `getHoursBadge` call sites (maint task list, DUE AT HRS detail badge, hours-overdue inline pill, equipment-grouped task list, urgency panel, kanban column, paperwork tab). Bonus VesselSetup fix: per-engine task `last_service_hours` baseline uses linked engine's hours. **Phase 2F follow-up** caught and shipped same-session: equipment-card view also rewritten — header builds info line from live engine state instead of stale notes blob; expanded task panel shows DUE AT HRS countdown with urgency color (Garry verified live: '480 hrs' header, '500h (20 to go)' yellow). **Phase 2G — engine identity backfill banner:** `isAutoBackfilled` check; banner on My Boat tab with "Add engine details" CTA; bottom-sheet modal with per-engine make/model/year/HP/fuel inputs. Targets only 6 vessels (incl. Garry's "Irene"). **~445 LOC for a feature that dead-codes itself once those 6 backfill** — flagged as tech debt; should be re-exposed as a permanent "Edit details" affordance on engine equipment cards. **Phase 3 (legacy column drop) deferred** as low-priority hygiene. **(2) LISTS feature rebuild — 4 surfaces, no beta gate.** Discovery surfaced significant prior Lists scaffolding (LandHoShell + `useBetaFeature` hook + NeedToBuy/Supplies/Haulout sub-views, ~3000 LOC) built to an OLD plan that Garry had killed: three-state lifecycle, `vessel_locations` managed table, `requires_haul_out` lens, beta gate. Garry on the locked revised scope: 4 parallel simple lists (Parts default / Supplies / Grocery / Haulout), no inbox, no aggregation, no Land ho, no beta gate — go live to all. **Lists Session 1:** 3 schema migrations applied to prod (`supplies.completed_at` nullable, `grocery_items` table, `haulout_items` table — both new tables RLS-enabled with the standard `vessel_id IN (SELECT get_my_vessel_ids())` policy matching saved_parts/supplies). New `ListsTab.jsx` 4-pill router (Parts default / Supplies / Grocery / Haulout). Stub `PartsView.jsx` + `SimpleListView.jsx`. Killed via `git rm`: `LandHoShell.jsx`, `hooks/useBetaFeature.js`, `NeedToBuy/`, old `Supplies/`, old `Haulout/`. Bottom nav and Profile-to-header avatar were already in place from a prior session — Lists tab in slot 5, Profile in upper-right header. **Lists Session 2 — Parts surface:** reads saved_parts where `state IN ('needed','ordered')`, received items hidden. Row layout `[bubble] [name + meta line] [archive — only if ordered] [⋯ menu]`. Bubble: empty grey ring → tap → filled gold-with-check (state: needed → ordered, sets `ordered_at`). Tap row body opens URL if present, else opens action sheet. Archive icon (only when ordered): marks received with 3.5s undo toast. ⋯ menu opens action sheet (Open link / Edit / Delete). Edit sheet with vendor/price/url/sku/notes editable; source label shown read-only ("From: Replace impeller"). No FAB — Parts populates from existing AI-bookmark + supply-promote flows. **Lists Session 3 — SimpleListView:** generic component for Supplies/Grocery/Haulout (one component, three mounts via `tableName` prop). Schema contract (id, vessel_id, name, notes, completed_at, created_at) shared across all three; supplies' legacy NOT NULL columns (`in_stock`, `min_stock`) have DEFAULT 0 so `INSERT (vessel_id, name)` works without special-casing. **v1 had a FAB → Garry replaced with inline "+ Add item" row** at bottom of list (Things 3 / Apple Reminders pattern): inactive shows dashed circle + "+" + muted "Add item" text, tap activates input, Enter saves and stays focused for rapid entry, × or Escape cancels, blur leaves text intact. Bubble tap: optimistic remove + 3.5s undo toast, sets `completed_at`. Action sheet (Edit / Delete) reachable via row tap or ⋯. **Lists polish patch — same-session:** stronger active pill highlight (was barely visible in dark mode — `--brand-deep` resolves to medium navy on dark navy bg). Solid `var(--brand)` bg + white text + slight shadow + border = clear "I'm on Grocery" affordance. Plus optimistic insert in `addItem` — temp row appears in <16ms via state update before DB write, input clears + refocuses immediately, fire-and-forget DB write reconciles when it returns. `_isTemp` flag on temp rows disables bubble/menu until DB confirms (race protection if user double-taps fast). **(3) ADMIN LISTS METRICS.** 4 new queries added to `/api/admin/stats/route.ts` `Promise.allSettled` batch (saved_parts, supplies, grocery_items, haulout_items). `parsePrice()` helper handles "19.50" / "$19.50" / "" / null → numeric or default $1 per Garry's spec. `aggregateRows()` helper extracts row count + distinct vessels Set. New `lists` object in JSON response with `partsNeeded`, `partsOrdered`, `supplies`, `grocery`, `haulout` shapes. `AdminStats` interface extended; new "Lists" section in admin page between Product and System — 5-card grid: Parts Needed (count · `$value · N vessels`), Parts Ordered (count · `$value · N vessels`), Supplies (count · `N vessels`), Grocery (count · `N vessels`), Haulout (count · `N vessels`). **Tech debt acknowledged this session:** ~150 lines of duplicated primitives (UndoToast, ActionSheet, EditSheet, Field, SheetButton) across PartsView.jsx and SimpleListView.jsx — extract to `components/Lists/shared.jsx` in a polish session. Engine identity backfill banner ~445 LOC targets 6 vessels and dead-codes itself once filled — re-expose as permanent "Edit details" affordance on engine equipment cards. Phase 3 multi-engine cleanup (drop legacy `vessels.engine_hours`, `engine_hours_date`, `fuel_burn_rate` columns + orphan `engine_makes`/`engine_models` tables) deferred — irreversible, only do when confident no app code reads from them.

- **Apr 28 LATE EVENING session — coverage CTA pass + AuthOpenerProvider lift + branded transactional emails — 7 patches shipped to main.** Built directly on top of the morning landing-rewrite session. Five distinct workstreams. **(1) Coverage-first messaging + "Get Keeply Free" CTA propagated globally.** New H1 `"Every system. Every part. Every passage."` (gold styling on `passage.`); new subhead `"Maintenance, repairs, parts, documents, and logbook — connected. First Mate AI ready when you want a hand."`; new FinalCTA H2 `"Always ready to go."`; button copy flipped from `"Get started"` → `"Get Keeply Free →"` everywhere except paid tiers (Standard/Pro on `/pricing` deliberately left as `"Get started →"` since Free messaging would mislead). All `app/layout.tsx` metadata (title, description, openGraph, twitter) flipped to coverage-first — closes the "5 root-metadata strings still stale" item that was open in the messaging-audit backlog. Files: `Hero.tsx`, `FinalCTA.tsx`, `app/layout.tsx`, `SiteHeader.tsx`, `AboutClient.tsx`, `FeaturesClient.tsx`, `PricingClient.tsx`, `LandingPage.tsx`. **(2) Hero video + section cleanup.** Removed top trust strip (`"No credit card needed · Start free →"`) from `LandingPage.tsx`; removed `"Free to start · No credit card · Cancel any time"` subline from Hero (kept once on FinalCTA as the page's one closing reassurance). Hero converted to **2-column on desktop / stacked on mobile**: text left, phone-with-walkthrough-video right. H1 sized `clamp(40px,5.5vw,72px)` desktop / `clamp(36px,8vw,52px)` mobile. Drops `100vh` minHeight. **Deleted `components/marketing/sections/VideoShowcase.tsx`** — same video now lives inside Hero, would have rendered twice. **PricingTeaser highlight moved Pro → Standard** (matches `/pricing` page). `scrollToPricing()` helper removed (was only used by the deleted trust strip); `<div id="pricing">` anchor kept for `KeeplyApp.jsx:11218` link. **(3) Free CTAs skip plan picker, go straight to AuthModal.** All "Get Keeply Free" buttons now bypass PlanPickerModal entirely and open AuthModal directly in signup mode with `pendingPlan='free'`, mirroring the Free-tier `handlePlanClick` pattern from `/pricing`: sets `localStorage.keeply_pending_plan='free'`, clears any stale `keeply_pending_price_id`, opens AuthModal. AuthModal already handled `pendingPlan='free'` (line 741: skips the plan-summary upsell panel). **PlanPickerModal is now orphaned** — file still in repo, `?plans=1` URL param still parsed defensively in `useAuthRedirects.ts`, `showPlanPickerOnMount` field still in `AuthRedirectsResult`. ~80 lines of dead code total; cleanup debt deferred to a hygiene pass. **(4) Lifted AuthModal to layout-level provider — the "no-blink" fix.** Off-homepage CTAs (SiteHeader on `/about`, `/features` bottom CTAs, etc.) previously used `<a href="/?signup=1">` — caused a visible nav-flash → landing page → modal sequence that "felt like a broken site." Architecture: new `components/auth/AuthOpenerProvider.tsx` lazy-loads AuthModal at layout level, exposes `openSignup(plan?)`, `openLogin()`, `openRecovery()`, `close()`; provider listens for `SIGNED_IN` event and closes modal on `/`, hard-navs to `/` for off-homepage signups. HomeClient drops its own AuthModal/PlanPickerModal mounting; consumes provider via `useAuthOpener()` hook; keeps auth state machine + Stripe-pending dispatch + KeeplyApp lazy mount. SiteHeader / About / Features CTAs converted from `<a href="/?signup=1">` to `<button onClick={openSignup('free')}>` — no nav, in-place modal. **Tradeoff Garry accepted:** post-signup blink (feels like normal loading) vs pre-signup blink (felt broken). **(5) CRITICAL FOLLOWUP — modal close + login getting stuck.** After Patch 4 deployed, clicking X on the login modal did nothing; logging in got stuck on the success screen. Root cause: HomeClient was using `const auth = useAuthOpener()` and putting the whole `auth` object in a useEffect dependency array. The provider's context value (memoized) recreates whenever `isOpen` or `isRecovery` flip — so every modal toggle changed the `auth` reference, re-firing HomeClient's URL param effect, which still saw `initialAuthMode === 'login'` (set on first mount via `?login=1` nav, never cleared) and called `openLogin()` again. Modal would close for one render, then re-open immediately. Fix: destructure individual functions from `useAuthOpener()` — they're `useCallback([])`'d in the provider, so they're stable across renders. Effects depending on stable functions don't re-fire on toggle. Also dropped the `authRef` indirection (was working around the wrong problem); listeners now call `openRecovery()` directly from closure with eslint-disable for missing dep (intentional one-shot mount effect). Verified live: X closes modal cleanly, login flow completes. **(6) Branded password reset email** — Supabase Dashboard → Authentication → Email Templates → Reset Password. Subject: `"Reset your Keeply password"`. Design: navy `#071e3d` header with `"Always ready to go."` gold kicker, gold `#f5a623` CTA button, copy-paste fallback link, neutral grey footer. No images = better deliverability (no "show images" prompt). Spam-friendly: single CTA, no urgency words, real plaintext fallback. Supabase dashboard config — no code deploy. **(7) Branded `/api/send-verification` email matching the password-reset look.** Rewrote the `html` template body in `app/api/send-verification/route.ts` to match: navy header + `"Always ready to go."` kicker, gold CTA, navy "Why verify?" callout. Preserved ALL server logic: token gen, app_metadata persistence, escapeHtml helper, Resend send, sender, reply-to, subject. Added `— Keeply / Always ready to go.` sign-off to plain-text fallback. Deployed; confirmed visually correct. **Deferred this session:** Magic Link, Confirm Signup, Change Email Address Supabase email templates — Garry's call. **Procedure violation acknowledged:** Garry called out pasting code in chat instead of delivering files via the standard Copy-Item workflow. Reason: filesystem tools dropped mid-session and only Chrome browser tools were available for the back half. Should have flagged the limitation upfront instead of working around it. Memory edits #26 and #30 updated to lock the deploy contract: explicit per-file `Copy-Item` from `C:\Users\garry\Downloads\` (never `$env`, never `-Recurse`); end of session determined by Garry, not Claude. Both rules now lifted to top of this doc under Operating rules. **Cleanup debt accumulated this session (file in next session if Garry wants):** `PlanPickerModal.tsx` orphaned file no longer referenced anywhere; `?plans=1` URL param parsing in `useAuthRedirects.ts` orphaned but still parsed defensively; `showPlanPickerOnMount` field in `AuthRedirectsResult` read by nothing. Total ~80 lines dead code.
- **Apr 28 session — landing-page architectural rewrite + PKCE password-recovery fix + lazy-load Supabase off marketing path + dead hero-poster preload removal — all shipped to production main.** Five distinct bodies of work in one long session. **(1) Marketing rewrite shipped.** `components/LandingPage.jsx` extracted from the KeeplyApp monolith into 8 typed section components at `components/marketing/sections/` (Hero, VideoShowcase, SocialProofMarquee, HowItWorks, Testimonial, CoverageChips, PricingTeaser, FinalCTA), driven by `components/marketing/LandingPage.tsx` (148 lines, **pure presentation, zero Supabase imports**), plus a `components/marketing/PhoneScreenshot.tsx` shared phone-mockup component. Authentication UI extracted in parallel into `components/auth/` (AuthModal.tsx, PlanPickerModal.tsx, useAuthRedirects.ts hook for URL param handling). `components/HomeClient.tsx` rewritten to own all modal state (showAuth, authMode, isRecovery, showPlanPicker, pendingPlan) and lazy-load both KeeplyApp and AuthModal/PlanPickerModal via `next/dynamic`. **Deleted:** the legacy `components/LandingPage.jsx` and the dead `components/AuthScreen.jsx` (latter had been completely superseded by AuthModal). Net diff: ~+2754 / −3318 lines, structure dramatically clearer. KeeplyApp.jsx still 7,800 lines but now imports only AuthModal-via-HomeClient instead of having marketing logic inline. Each section component has its own props surface and is independently testable. **(2) Walkthrough video shipped.** 33-second 376×720 H.264 walkthrough video at `public/videos/walkthrough.mp4` (240 KB), embedded in the new VideoShowcase section's PhoneScreenshot. Replaces the static phone screenshot pattern from the previous landing iteration. **(3) SiteHeader/SiteFooter visibility — auth-aware fix via `force` prop.** Both components had a HIDE_ON list including `pathname === '/'` (so they hide on the home route, where the marketing rendered its own header/footer). Adding `force?: boolean` prop bypasses HIDE_ON; HomeClient renders `<SiteHeader force />` and `<SiteFooter force />` inside its guest branch and inside its recovery-modal branch. Authed users see no global header (KeeplyApp owns its own chrome). Verified working in browser via `document.querySelectorAll` returning expected counts in both states. **(4) PKCE password-recovery saga — 4 iterations, real root cause uncovered.** The `forgot password → email link → set new password` flow was completely broken on production. First fix attempted to preserve the URL hash through `useAuthRedirects.replaceState` and add a forgotMode UI state; partial — Flow A (in-app reset) passed, Flow B (clicking the email link) still broke. Second fix added `if (isRecovery) return <recovery UI>` shadow at the top of HomeClient's render branch — still broken. Third iteration finally revealed the actual cause: `components/supabase-client.js` line 7 uses `flowType: 'pkce'`, and **per Supabase docs, PKCE delivers password-recovery sessions as `?code=XXX` query params (NOT `#access_token` hash fragments) AND the `PASSWORD_RECOVERY` event does NOT fire reliably under PKCE**. All three previous fixes assumed the event would fire. Final fix is a custom URL marker pattern: `AuthModal.resetPassword` uses `redirectTo: '/?keeply_recovery=1'`, useAuthRedirects detects the marker and sets `initialRecovery=true` while preserving the `?code=` for Supabase to consume, HomeClient applies `initialRecovery` on mount → `isRecovery=true` → recovery modal renders independently of any Supabase event. A follow-up fix corrected marker cleanup: original cleaned the `keeply_recovery` param only when `?code=` was already gone, which left it stuck after Supabase consumed the code, causing the recovery modal to reopen on refresh after sign-out. Final version cleans `keeply_recovery` selectively on first read regardless of code presence. End-to-end verified by Garry: set new password → into app → sign out → refresh (no modal reopens) → log back in with new password, all clean. **(5) Lazy-load Supabase off marketing critical path — Fix D from Apr 27 backlog, now shipped.** `HomeClient.tsx` previously did `import { supabase } from './supabase-client'` synchronously, dragging the 205 KB Supabase auth chunk into the marketing bundle even on fresh guest visits. Surgery: removed the top-level import; added `supabaseRef`, `subscriptionRef`, `supabaseLoadingRef` refs; added `ensureSupabaseLoaded()` lazy loader that dynamic-imports supabase and sets up the `onAuthStateChange` listener exactly once (idempotent — repeat calls return the cached promise/instance). The mount effect now only triggers `ensureSupabaseLoaded` if `hasAuthHint()` returns true (returning user) OR the URL contains `?code=` (PKCE callback) OR `?keeply_recovery=1` (recovery flow); fresh guest → setState('guest') immediately, NO Supabase load. A second effect on `[showAuth]` eagerly loads Supabase when the user opens AuthModal, so SIGNED_IN events from the modal flow back to HomeClient state. **Verified empirically post-deploy:** chunks/manifest analysis confirmed 205 KB main supabase + 539 KB KeeplyApp chunk + 80 KB supabase auxiliaries (4 chunks) all moved off the marketing critical path; only a 27 KB chunk with a "supabase" string match remains (likely a URL constant, not the auth client itself). Total scripts on marketing went from 906 KB → 700 KB decoded (-206 KB exactly = the supabase auth chunk). **(6) Dead hero-poster preload removal — pure waste, removed.** Console warning during the perf investigation flagged `hero-poster.jpg` as preloaded but not used. Investigation showed two `<link rel="preload" as="image" media="..." fetchpriority="high">` tags in `app/layout.tsx` (lines 71-84) for `hero-poster.jpg` and `hero-poster-mobile.jpg` — leftover from the Apr 27 hero design where the image WAS the LCP. Today's marketing rewrite swapped to text-hero ("Always ready to go.") + phone-mockup-with-video. Search of `components/` and `app/` for `hero-poster` references returned ONLY the two preload tags — neither image is rendered anywhere in the new design. Removed both `<link>` blocks; the H1 text is the LCP now and renders without needing an image preload. Effect on slow 4G mobile: the browser was spending 4-8 seconds dutifully downloading 374 KB (187 + 151) of unused hero posters at high priority, blocking actual LCP element from painting. **PageSpeed result, end-to-end this session:** mobile Performance score 49 → 78, single jump after the preload removal. LCP 10.2s → 2.9s. TBT 680ms → 500ms. Speed Index 6.1s → 2.8s. CLS 0.005. Accessibility 88, Best Practices 100, SEO 100. The lazy-load Supabase fix gave a smaller bump on its own (49 → 55 mobile, mostly from TBT improvement) — the LCP win came from killing the dead preload. **Files modified this session, all on production main:** `components/HomeClient.tsx` (rewrite + lazy-load surgery), `app/layout.tsx` (preload removal), `components/marketing/LandingPage.tsx` (NEW), `components/marketing/PhoneScreenshot.tsx` (NEW), `components/marketing/sections/*.tsx` (8 NEW files), `components/auth/AuthModal.tsx` (NEW), `components/auth/PlanPickerModal.tsx` (NEW), `components/auth/useAuthRedirects.ts` (NEW), `public/videos/walkthrough.mp4` (NEW). Deleted: `components/LandingPage.jsx`, `components/AuthScreen.jsx`. **Open / next session priorities:** (a) **Smoke test on prod** in incognito — marketing renders, click Log in → AuthModal opens → sign in → KeeplyApp loads → sign out → fresh marketing. The lazy-load is the riskiest piece because it changed the auth state machine; field-test before more changes. (b) **PageSpeed continuation, low-priority.** 78 is solidly respectable; pushing into 80s+ would mean killing autoplay on the walkthrough video on mobile (240 KB freed, ~5-10 points) and preloading the brand font (Satoshi) so the H1 doesn't wait for it (~3-5 points off LCP). Diminishing returns above 80 — 78 is a fine ship point. (c) **Cleanup the unused hero-poster.jpg / hero-poster-mobile.jpg files in `public/images/`** — they're committed to the repo but referenced nowhere now that the preload tags are gone. ~340 KB of repo bloat. Not breaking, just hygiene. (d) **The 27 KB still-on-path chunk with "supabase" string** — investigate whether it's just a URL/key constant or actual unused supabase code. Likely the former; even so, identifying and removing dead Supabase imports from anywhere in the marketing tree would tighten further. (e) **CrUX field data check after 24-48 hours** — lab score of 78 means real-user metrics should follow up; if not, lab vs field divergence to investigate.
- **Apr 27 evening session — perf fixes shipped to production main, PostHog defer attempted and reverted.** Goal: attack the LCP 7.7s / Performance 44 finding from earlier in the day. **Shipped to production (live):** (1) **Hero video removed** — `sailing-hero-web.mp4` (6.2 MB autoplay) deleted plus orphans (`sailing-hero.mp4` 14 MB, `hero-sunset.jpg` 4.4 MB); replaced with two static photos from S/V Irene Mexico passage: `public/images/hero-poster.jpg` (187 KB, 1600×900 landscape, sun rays mid-frame, sail upper-right) and `public/images/hero-poster-mobile.jpg` (151 KB, 1080×1440 portrait), responsive via `<picture>` with `(max-width: 767px)` media query; (2) **Cache headers** added in `next.config.ts` — 1-year immutable for `/videos/`, `/images/`, `/fonts/`; (3) **Service-worker `.catch()`** added to `navigator.serviceWorker.register('/sw.js')` in `app/layout.tsx` (was firing benign rejections to PostHog on every visit); (4) **Hero LCP image preload** — two `<link rel="preload" as="image" media="..." fetchpriority="high">` tags at the very top of `<head>` in `app/layout.tsx`, gated by viewport so each visitor preloads exactly one image; (5) **SSR refactor (the architectural fix)** — `app/page.tsx` converted from `'use client'` to a server component that just renders `<HomeClient><LandingPage /></HomeClient>`; new `components/HomeClient.tsx` owns the auth state machine with three states (`'guest' | 'pending' | 'authed'`); initial state is `'guest'` on both server and first client render so SSR HTML matches hydration cleanly; `hasAuthHint()` synchronously scans localStorage for any `sb-*-auth-token` key in useEffect to swap to `'pending'` (hides marketing flash for returning users); `firePendingStripe` lifted verbatim into `HomeClient`. Result: LandingPage HTML body went from 15 KB (empty React shell) to 42 KB (real HTML with hero `<img>` + H1 + sections), **"LCP request discovery: discoverable in initial document" diagnostic flipped from ✗ to ✓**; (6) **GTM library load deferred** — `<script async src="...gtag/js...">` removed from `<head>` in `app/layout.tsx`; inline `gtag()` queue + config calls stay in head; new inline script schedules library injection via `requestIdleCallback(_ldGtag, { timeout: 4000 })` with `setTimeout(_ldGtag, 2000)` Safari fallback. Forced-reflow time on GTM scripts went from 228ms (114+114) to 62ms (31+31) — 75% reduction in PageSpeed diagnostics; (7) **`cdn.fontshare.com` preconnect** added (with `crossOrigin=""` because font requests are CORS by default) — `api.fontshare.com` was already preconnected for the CSS endpoint, but actual woff2 files load from `cdn.fontshare.com`. **REVERTED (not on production):** Fix C — PostHog defer. Attempted to dynamic-import `posthog-js` inside `requestIdleCallback`, with a queue layer in `lib/posthog.ts` so all `capture()`/`identify()`/`trackException()` calls fire or queue cleanly. Touched 5 files: `lib/posthog.ts` (full rewrite to queue+lazy-load), `lib/analytics.ts` (replaced direct `posthog.capture()` with queue helper), `components/PostHogProvider.tsx` (deferred init), `components/AuthScreen.jsx` (replaced direct posthog imports with `capture`/`identify` helpers), `components/LandingPage.jsx` (same). Mechanically the deferral worked — PostHog moved from synchronous chunk to a 175 KB chunk loading at ~795ms — but production score went from ~63 to two consecutive 53/60s. **Honest read: the defer pushed 84 KB of PostHog CDN files (`posthog-recorder.js` 51 KB, `surveys.js` 33 KB, `exception-autocapture.js`, `dead-clicks-autocapture.js`, `web-vitals.js`, `config.js`) from the pre-FCP window into the post-FCP TBT measurement window, where Lighthouse weights them more heavily.** Reverted via `git revert HEAD`. Verified via Chrome MCP after revert: 175 KB deferred chunk gone, PostHog back inline at 364→1189ms alongside the other big chunks. **Diagnosed during session but NOT fixed:** Staging Stripe checkout was 500-ing (`{"error":"Stripe not configured"}`) because `STRIPE_SECRET_KEY` was set for Production only, not Preview. Pattern matches the SUPABASE_SERVICE_ROLE_KEY First Mate bug from earlier in the month. Garry fixed it (ticked Preview checkbox) and triggered a redeploy via empty commit `git commit --allow-empty -m "redeploy: pick up STRIPE_SECRET_KEY on Preview"` (the dashboard redeploy button initially didn't take). After fix, `/api/stripe/checkout` on staging returns 200 with a real `cs_live_*` Stripe Checkout URL. **NOTE: staging is using a `sk_live_` key** (same as production). Long-term: swap for `sk_test_` so staging signups don't create real customers in the live Stripe account. Test card `4242 4242 4242 4242` only works in test mode. **Production critical-path JS chunks (decoded sizes, all loading 364→1500ms range):** 227 KB React DOM, 206 KB **Supabase** (the next big lever), 171 KB PostHog, 140 KB Next.js core, plus ~150 KB across smaller page-specific chunks. Total ~890 KB. Identified by fetching chunk URLs and string-matching for library signatures (`/@supabase|createClient/`, `/posthog|autocapture/`, `/usePathname|useSearchParams/`). **Supabase is the biggest remaining deferable thing** — `components/supabase-client.js` calls `createClient()` at module load time, and BOTH `LandingPage.jsx` and `HomeClient.tsx` do `import { supabase } from './supabase-client'` synchronously, so the library + a client instance ship on every marketing visit even though most never sign up. **Score variance was the dominant signal all night and I let it mislead the work.** Single PageSpeed lab runs ranged 53→75 on the same code with no deploys between them. Web.dev's own docs acknowledge ±10-15 point variance on shared headless infra; TBT is the most CPU-sensitive metric and most variable. Garry corrected an important misread mid-session: the 73 median was on staging only, not production — production was never above the high-50s/low-60s. Tonight ended without a calibrated production baseline. **Architectural facts that ARE verified independent of score:** SSR LandingPage (hero `<img>` ships in initial HTML body, 15→42 KB), GTM forced-reflow 228→62ms, "LCP discoverable in initial document" ✓. Those are real. Whether they moved the *score* meaningfully is what 5+ runs and CrUX field data over 24-48 hours will tell us. **Files modified tonight (all currently on production main):** `app/layout.tsx`, `app/page.tsx` (now server component), `components/HomeClient.tsx` (NEW), `components/LandingPage.jsx` (hero `<picture>`, video removal already from earlier today), `next.config.ts`. **Files added then reverted:** none — the PostHog defer touched the 5 files above and was undone via `git revert HEAD` so they're back to pre-Fix-C content. **Open / next session priorities:** (1) **Calibrate real production baseline** — 5+ PageSpeed mobile runs on `www.keeply.boats` spread across an hour, take the median; cross-reference with CrUX field data after 24-48 hours of accumulated traffic. Without this number, no further perf decisions are sound. (2) **Fix D — Supabase defer on marketing path.** Lazy-load supabase client only when needed (URL has `?code=` for OAuth callback, user clicks Get Started/Login, etc.). 206 KB off the critical path. Higher complexity than tonight's work — auth flow code, OAuth callback edge cases, must NOT regress the Stripe redirect after signup. Plan/scope first in fresh head, no code without a written design. (3) **Optional Fix C v2 — selective PostHog disable on marketing only.** Initialize PostHog with `disable_session_recording: true` and `disable_surveys: true` when pathname is `/`, `/about`, `/features`, `/pricing`, `/faq`, `/contact`, `/support`. Re-enable when authed and in-app. Drops 84 KB of PostHog CDN files only on routes where they have low value anyway. CONTEXT note from Apr 27 says autocapture is on for "ad-traffic learning, revisit in 90d" — recordings on marketing have especially low value vs perf cost. (4) **Stripe Preview key swap** — switch staging `STRIPE_SECRET_KEY` from `sk_live_` to `sk_test_` so staging signup flows can be fully tested with `4242` test card without real charges. (5) Lower priority: the 285 KB unused-JS finding includes ~120 KB of genuine first-party bloat that could be cleaned up (Lucide icons that aren't visible above-the-fold, KeeplyApp.jsx imports leaking via shared utils). Post-launch tech debt, not blocking. **Don't** chase single PageSpeed scores; they're noisy. Trust architectural diagnostics (network dependency tree, LCP request discovery, forced reflow) and CrUX field data.
- **Apr 27 session — marketing pass + Repairs UI bug + PostHog live + Vercel cost fix + mobile perf diagnosed (the new headline finding).** Long session, 10+ deploys, but the genuine takeaway is a confirmed **mobile performance crisis on the homepage** that explains every weak engagement signal we've been guessing about. PageSpeed Insights mobile (Slow 4G, Moto G Power emulation): **Performance 44, LCP 7.7s, FCP 3.6s, TBT 930ms, total page payload 7.2 MB.** Diagnostics flag enormous network payloads (7,216 KiB), 285 KiB unused JS, 89 KiB cache savings, 44 KiB legacy JS, render-blocking requests, 1.9s of JS execution time, 2.7s main-thread work. **Real-world impact confirmed via PostHog:** Miami iPhone Safari user (es-US, T-Mobile cellular, North Miami Beach 33162) clicked the brand campaign ad, hit `/`, lasted **1.246 seconds** with 0 scroll and 23.9% max content visible, bounced before LCP could even fire. They literally couldn't have read the page. **Fix priority for next session (in order):** (1) hero video / oversized images — click "Avoid enormous network payloads" in PageSpeed for the file list; the autoplay `/videos/sailing-hero-web.mp4` is the prime suspect, options are replace-with-poster, move-below-fold, or lazy-load; (2) reduce unused JavaScript — likely KeeplyApp.jsx imports leaking onto the marketing path, or Stripe/Anthropic SDK init firing on landing; (3) cache lifetimes (vercel.json or next.config.ts tweak); (4) legacy JS polyfills going to modern browsers. Target: LCP under 3s, performance score 70+. Realistic scope: 2-4 hours focused work. **Performance fix is now the highest-leverage thing on the board** — every conversation about ad copy, messaging, AvailabilityStrip etc. assumed users were *seeing* the page; they aren't. **PostHog is fully wired and confirmed working** — `app/layout.tsx` mounts `<PostHogProvider>`, `lib/posthog.ts` config, typed event helpers (`trackCtaClicked`, `trackSignupStarted`, `trackSignupCompleted`, `trackVesselCreated`, `trackFirstMateUsed`, `trackUpgradeClicked`, `trackUpgraded`), and existing AuthScreen captures (`signup_started`, `signup_completed`, `login_completed`). **Two changes today:** (a) `autocapture: false → true` in `lib/posthog.ts` for ad-traffic learning phase (revisit in 90d); (b) session recording confirmed ON via PostHog project Settings → Recordings (no code change). **Garry's Distinct ID `AkwQ35fabXA89vLQQdtTzz67PVxf8NDsi5F6UZFqRSiu`** added to PostHog Settings → Internal Users so future "Filter out internal" toggles actually filter him. **Privacy policy updated** (`app/privacy/page.tsx`): added PostHog row to processor table in Section 3, rewrote Section 6 (Cookies — old text "we do not use third-party analytics cookies" was now false), added new Section 7 "Analytics and Session Recording" with full disclosure (pageviews, events, session recordings, masking, geographic/device data, opt-out via email + DNT honored, link to posthog.com/privacy), renumbered subsequent sections, effective date bumped to 2026-04-27. **Vercel billing finding:** April bill was $120 — driven entirely by **Turbo build machine tier** (15h 42m at premium rate, 95.7% of build minutes; Standard/Included was only 4.3%). Root cause: project was on Turbo, probably auto-promoted or accidentally enabled. Action item Garry will execute: **Vercel project Settings → General → Build Machine → switch to Standard.** Expected May bill ~$20-25 (Pro subscription only; everything else inside included quotas). All other usage trivial — function invocations $0.60, fluid memory $0.06, etc. **Marketing site work shipped today:** **About page** — replaced literary-reconstruction founder note with Garry's authentic voice from `About.docx` (Mexico departure 2025, ten years prepping the boat, "camera roll became nothing but photos of broken bolts," "If not, then we just made another app that we will discard" blockquote, Fairwinds and a following sea sign-off); new founder photo at `public/images/about/founder-portrait.jpg` (helm/spinnaker shot, 246 KB compressed from 5.1 MB) replacing the cropped cockpit-selfie; watermaker example replaces engine/impeller in card 03 (engines are AI weak spot, watermakers are AI strong); Nordhavn 63 added in Upgrader card replacing redundant "serious motor yacht"; "what Keeply isn't" counter-positioning block removed (74 lines); CTA tightened to *"Free for as long as you want. No credit card. Sign up now and be onboard in 3 minutes."* **Features page** — hero now *"Everything to manage your boat, in one place."*; full reorder to match user chronology: Setup → Documents → Equipment → Maintenance → Repairs → Parts → Logbook → First Mate (was: Maintenance → Repairs → Parts → Logbook → Documents → Equipment); all 8 visuals replaced with **real product screenshots in phone-bezel mockups** via new `PhoneFrame` component + `makeShot()` factory; old animated visuals (`MaintenanceVisual`, `PartsVisual`, `LogbookVisual`, `MyBoatVisual`, `FirstMateVisual`) imports dropped; new Setup section copy reflects 3-min/30-min framing from `Feature_Page_Changes.docx`; Equipment title now *"Every piece of equipment on your boat with its own card"* (Garry's line); Maintenance title *"Every system. Every interval. Nothing missed."*; Parts dropped named retailers ("Fisheries Supply, West Marine, Defender") for "major marine retailers" + shopping-list mention; First Mate closer upgraded from demoted "Help when you want it" to *"Your vessel knowledge, in one conversation."* Screenshots compressed to 720px wide, JPEG q78 progressive, 309 KB total for all 8 (saved to `public/images/features/`: setup, documents, equipment, maintenance, repairs, parts, logbook, first-mate). **AvailabilityStrip component** shipped — new `components/marketing/AvailabilityStrip.tsx` mounted in both `SiteFooter.tsx` (covers all public pages except landing) and `LandingPage.jsx` footer (separate from SiteFooter). Three pills: Web (Available now), iOS (Coming this summer), Android (Coming this summer). Addresses the implicit-promise gap created by phone mockups all over the site with no App Store presence. *Note: "this summer" commits to a date publicly — if the rebrand-and-store-submission slip past August, this strip needs updating.* **Repairs UI bug fixed in TWO passes** (caught from a Features-page screenshot showing truncated part names): **Pass 1** — My Boat tab repair card was rendering its expansion as a **flex sibling** of the summary column instead of nested inside it, so expanded parts panel sat to the right and got squeezed. Moved the closing `</div>` from L13103 to after the expansion at L13636 — turns side-by-side into stacked. **Pass 2** — Garry then noticed the bookmark and open-vendor action icons were still missing from each part row. Root cause: outer card has `display: flex; overflow: hidden`, the flex:1 column inside it was missing `minWidth: 0`, so any wide content inside (long Jabsco part names) blew the column wider than the card and `overflow:hidden` clipped the right edge — taking the action buttons with it. **Pattern lesson worth carrying forward: `flex: 1` and `minWidth: 0` always go together when content can be wide and a parent has `overflow: hidden`.** Both fixes are in `components/KeeplyApp.jsx`. The same urgency-stripe-with-overflow pattern is unique to L12784 (My Boat); the other 4 repair-render sites (L8602 Lists, L16345 Equipment-tab, L19819+L21209 Repairs-standalone) already stack correctly. Bringing the urgency stripe to those 4 sites is filed as a separate cosmetic pass, not done now. **Ad copy rewritten and aligned to site** — old non-brand ad promised "Set up in 60 sec" and described First Mate as a tracker; both contradicted the site. New: H1 Boat Maintenance App, H2 Every System. Every Passage., H3 Free to Start - 3 Min Setup, plus three rotation backups (Ask First Mate AI, Track Every System, Made by Boaters For Boaters). Two descriptions: *"AI that knows your boat. Ask First Mate when service is due, what parts fit, what's overdue."* and *"Onboard in 3 minutes. Every system, every part, every passage. Free — no credit card."* All 4 sitelinks rewritten (Features, Pricing, About, FAQ — Contact and Support dropped as not-conversion pages). Final URL gained UTMs: `?utm_source=google&utm_medium=cpc&utm_campaign=non-brand`. Display path set to `setup` / `3-min`. **Campaign hygiene** verified: 11 locations targeted (PNW + Florida + ICP regions), English only, Display Network was already off, **Google Search Partners checkbox unchecked today** after PostHog revealed a brand-campaign click coming in via `youtube.com` referrer with `gad_source=2` (search partner traffic was bleeding to YouTube search bar). **PostHog data review (the substance of the night):** ~17 visitors today on the live site with autocapture+recording now on, 5 paid clicks total per Google Ads dashboard, examined three distinct paid sessions in PostHog. (1) `019dd0d6` Wichita iPhone Chrome iOS 26.4.2, brand campaign — almost certainly Garry's network or geocode-fallback artifact (37.751,-97.822 is the geographic centroid of contiguous US, suggests imprecise IP geo). (2) `019dd09f` **Miami iPhone Safari iOS 18.7 es-US T-Mobile cellular, brand campaign** — the killer data point: 1.246 sec, 0 scroll, 23.86% max content visible, bounced before page rendered. ICP-aligned, brutal bounce. (3) `019dd0a5` **Seattle Android Chrome 147, brand campaign via youtube.com referrer** — PNW ICP, came in via YouTube search bar (this is what caused the Search Partners checkbox decision). No engagement metrics captured (mobile back button skipped beforeunload). **Service worker registration is firing benign rejections on every visitor** (`navigator.serviceWorker.register('/sw.js')` at `app/layout.tsx` L149 has no `.catch()` so promise rejections become unhandled exceptions captured by PostHog). Page works fine; exception is cosmetic noise in the PostHog feed. Easy fix: add `.catch(() => {})`. Filed, not blocking. **What is NOT done:** The "messaging audit" task is still open — root metadata in `app/layout.tsx` still has 5 stale strings ("AI-powered" / "Your vessel's First Mate, always ready") that don't match the now-aligned About + Features. Still deferred to fresh head; lower priority than perf work. **Performance fix dwarfs everything else right now.** *(Apr 28 evening update: messaging audit task is now CLOSED — root metadata flipped to coverage-first.)*
- **Apr 26 late PM session — full marketing site rework + deploy policy change + nav-contrast fix + About bg photos + FAQ schema.** No new product code; entirely marketing surfaces, infra policy, and SEO. **Landing page restructured** (LandingPage.jsx 5927→2432 lines): 5-card animated FEATURES section + mid-block visual + 3 value-prop cards + entire pricing block (478 lines) + ~2500 lines of orphaned visual code all removed; visuals extracted to `components/marketing/FeatureVisuals.jsx` (2845 lines, 7 visuals + useWhenVisible helper); new `/features` route at `app/features/page.tsx` + FeaturesClient.tsx with 6 sections matching home chips plus First Mate closer; Features added to nav (SiteHeader, LandingPage desktop+mobile menu, footer). **Hero #2 evolution:** first attempt was animated dark phone mockup (HeroAppLoop.jsx, 1139 lines, 5 stylized scenes on 7s rotation) — replaced with PhoneScreenshot wrapping a real S/V Irene My Boat screenshot, then iterated to **3-phone hybrid layout**: desktop shows three phones side-by-side (First Mate left tilted -4°, My Boat center elevated, Logbook right tilted +4°) each with caption underneath ("Talk to your boat" / "Always keep you informed" / "Collect the information you want"); mobile renders just the My Boat phone. Real screenshots: `hero-my-boat.jpg`, `hero-firstmate.jpg`, `hero-logbook.jpg` (each ~50KB). **About page background photos:** 4 unused S/V Irene photos compressed 14× (15.3MB→1.1MB; 1920px wide, JPEG q72, progressive) into new `public/images/about/` subfolder; pacific-bay (hero, 70% navy overlay), cockpit-selfie (founder note, 85%), engine-room (what we're building, 88%), spinnaker (who it's for, 85%). Pattern matches contact/support pages: photo at `position:absolute; inset:0`, navy overlay, content lifted via `position:relative; zIndex:1`. Cockpit-selfie rotated CCW, engine-room + spinnaker rotated CW (camera EXIF orientation issues). **About tone scrub:** five places aligned to coverage-led messaging — hero sub ("vessel intelligence platform"→"where your boat's whole record lives"), founder note closing (bolded coverage list, FM demoted to "help when you want it"), three pillars rewritten coverage-first, OG/Twitter metadata. **Bottom nav contrast fix** based on tester feedback ("buttons blend with background"): 4 standard buttons icon opacity 0.28→0.55, all 5 button label fontSize 7.5→9.5, My Boat label opacity 0.5→0.75; active gold + center elevated button untouched. 10 line edits all within nav block (L7481-7746 of KeeplyApp.jsx). **Deploy policy reversed:** old "always staging then prod" was paying ceremony — 14+ deploys today, never tested staging URL between pushes; one Features CTA fix was lost because the second push never ran. New default is **single push to main**; staging-first ONLY for schema migrations to prod, payment flow (Stripe/checkout), auth flow (signup/login/verify), or anything writing irreversible production data. Mental test: "if this breaks, can I push a fix in 60s and forget it?" Yes→main. No→staging first. Memory + this doc + future deploy commands all reflect new default. **FAQ SEO work** (highest-ROI of the session): page split into server `page.tsx` + `FAQClient.tsx` (was `'use client'`-only with no metadata, inheriting only root meta — meaning /faq competed with home for the same title/description in SERPs); new dedicated FAQ metadata (title "Boat Maintenance App FAQ — Pricing, Features, AI | Keeply", canonical URL, OG+Twitter); **FAQPage JSON-LD schema** auto-generated from existing 45-Q&A SECTIONS array, rendered as inline `<script type="application/ld+json">` tag, validates clean at Google Rich Results Test (1 valid item detected, 45 questions inside). SoftwareApplication schema also already shipped in `app/layout.tsx` (validates with non-critical aggregateRating warning — deferred until App Store reviews exist; self-published ratings are spam-flag risk). SoftwareApplication description aligned with new coverage-led copy. **5 other root-metadata strings still stale** (root description, OG title/desc/alt, Twitter title/desc all reference "AI-powered" / "Your vessel's First Mate, always ready") — flagged as "messaging audit" task because picking the canonical Keeply one-liner is a real strategic call, not just an edit; deferred to fresh head. *(Apr 28 evening update: messaging audit closed — all root metadata now coverage-first.)* **Network-debug rabbit hole:** Garry hit ERR_INTERNET_DISCONNECTED in DevTools, both normal browser and incognito. Wrong diagnosis: service worker (was clean), then dual Supabase clients warning (real but unrelated), then browser extensions (irrelevant), then AV (irrelevant). Actual cause: **Chrome DevTools Network throttling stuck at "Offline"** — sticky setting that survives reloads/tab-switches. Switched to "Fast 4G" → instant fix. Phone-on-same-wifi test correctly ruled out our infra; should have been my first question. **Real finding from the rabbit hole:** the "You're offline" anchor screen shipped in FirstMateScreen.jsx works as designed. The "click suggested question, nothing happens" symptom is a separate bug — `send()` in FirstMateScreen.jsx L374 silent-bails on `!context`; needs ~30-line UX patch (disable chips while context loads, show "Loading your boat's data...", console.warn on bail-out). Filed. **Dual Supabase clients also filed:** `components/supabase-client.js` (hardcoded URL/key, used by KeeplyApp) AND `lib/supabase.js` (env-var-based, used elsewhere) both call `createClient()` separately, producing the "Multiple GoTrueClient instances detected" console warning. Latent token-refresh-race risk; not tonight-blocking but worth a focused unification pass.
- **Apr 25–26 session — GoLive blockers closed.** Both pre-launch blockers identified and shipped: (a) document-scan partial-fields data-loss bug — scanning a second document (e.g., insurance) was wiping fields saved by the first (e.g., registration); (b) `/api/delete-account` had no auth check AND scoped its cascading deletes to `vessel_members`, meaning a crew member deleting their account would wipe the OWNER's vessel data. With 3 active member relationships in production, that risk was live, not theoretical. Both fixed. Self-delete verified end-to-end with `garry+test@keeply.boats`. **Real signups can now turn on whenever Garry is ready to triage Day-1 issues.**
- **Apr 26 PM (cont.) — Lists Session 5: First Mate context expansion + auto-pull + Need to buy sectioning.** Two deploys (`ad44941b` original, `7eed533` fix). **FM is now Lists-aware** — `fetchVesselContext` in both `FirstMate.jsx` (bottom-sheet) and `FirstMateScreen.jsx` (full-screen tab) fetches `saved_parts` and `supplies` (with `vessel_locations(name)` join) alongside the existing 5 tables. `tasks` mapper now passes `requiresHaulOut`. Server prompt builder (`app/api/firstmate/route.js`) now produces three new sections: **SAVED PARTS** (state breakdown + needed list), **SUPPLIES ON BOARD** (low-stock callout + capped 40-row inventory), **HAULOUT QUEUE** (combined task+repair list). MAINTENANCE and OPEN REPAIRS now exclude `requires_haul_out=true` items — they surface only under HAULOUT QUEUE — matching the principle that haulout work is not actionable in-water. BEHAVIOUR block updated to direct FM by question type ("what do I need to buy" → SAVED PARTS, "do I have an X" → SUPPLIES, etc.). **Need to buy is sectioned R&M vs General** — items with `source_type IN ('task','repair')` go under "Repairs & Maintenance"; everything else (including new 'supply' source) goes under "General." Section headers hide when empty. New `<SectionGroup>` component in `NeedToBuy.jsx`; `sourceTypeLabels` gained `supply: 'Restock'`. **Auto-pull on threshold breach** — `maybeQueueRestock(supply)` helper in `Supplies.jsx` fires after every successful write (`adjustQuantity` ± and both branches of `saveItem`). Trigger rule: `in_stock=0` always queues regardless of `min_stock` (out-of-stock = unambiguous "need more"); below-threshold rule still applies when `min_stock>0`. Dedup checks for any open `saved_parts` row with this `supply_id` AND state in `('needed','ordered')` — re-queueing is silent if already pending. Schema migration applied: `saved_parts_source_type_check` extended to allow `'supply'`. One-time SQL backfill ran for 5 supplies that were already at-or-below threshold pre-Session-5 (Lantern Fuel, toilet paper, Vinegar, Mercury Oil Kit, Raw Water Impellor) — they now appear in Need to buy under General with the Restock label. **Real learning:** when adding a new value to a domain-like text column, check `pg_constraint` for CHECK constraints — `SELECT DISTINCT` shows what exists, not what's allowed. The original Session 5 build saw `'task'` and `'repair'` in `saved_parts.source_type` via DISTINCT, assumed the column was free-text, and inserted `'supply'` rows that the DB silently rejected via the `saved_parts_source_type_check` constraint. The `console.error` in the helper caught the rejections but produced no user-visible feedback — so the bug looked like "nothing happens." Caught when Garry reported decrementing supplies to zero with no Need to buy entries.
- **Apr 26 PM session — Lists Sessions 1–4 + photo bug closed + service log photos + Mark-as-Done sheet redesign.** 14 deploys in a single sitting; full sequence below. **Nav rework first** (`a75446c`): FM chips removed; FM restored to bottom-nav slot 4 (was being subsumed by the persistent top bar in the previous session and people kept missing it); Lists moved to slot 5; Profile relocated to header avatar to free a slot. **Photo upload bug closed** (`647312a`, `29159b8`, `8143a18`): root cause was `capture="environment"` HTML attribute forcing camera-only on Android (no gallery picker). Removed across all 4 photo upload sites. Diagnostic toasts added for repro then ripped; only the breadcrumb in the catch block survived. **Service log photos** (`8143a18`): tapping Mark Done on a maintenance task now snapshots the active photos column into the log entry's `photos` JSONB and clears the active column — matches the "evidence of THIS service event" mental model rather than letting today's photos bleed into next year's service. Read-only lightbox in service history. **Mark-as-Done sheet redesign** (`e850510`): dropped the redundant Skip-note button, added inline photo capture (uploads to active task photos, snapshots into the log entry on Mark Done), single full-width primary CTA. **Lists Session 1** (`bcd79a6`): Land ho shell + `lists_waitlist` schema + `user_profiles.beta_features` text[] kill switch. **Lists Session 2** (`e2fca24` + `0f49abe`): Need to buy view live; new `saved_parts` table with needed/ordered/received state machine, source linkage to tasks/repairs, `get_my_vessel_ids()` RLS; bookmark icon wired into all 9 AI parts result surfaces via a small `savePartToList(part, source)` helper in KeeplyApp. The follow-up commit fixed a `supa()` POST+`return=minimal` interaction (HTTP 201 + empty body chokes `res.json()`); patched at the call site by dropping the prefer header rather than fixing the helper — see learning below. **Lists Session 3** (`ac74d14` → `923db52` → `de50d41` → `c2a1dc5`): Supplies inventory view + `supplies` schema; ListsTab gains a pill router (Need to buy / Supplies); locations promoted to a managed `vessel_locations` table with manage-sheet + ON DELETE RESTRICT + UI delete check (Garry: "countless times I rip the boat apart because I forgot where I shoved something"); SVG icons replace empty-state emojis; Mark received → Supplies handoff via a ReceiveModal that creates an inventory row and links via `saved_parts.supply_id`, with a Skip option preserving the no-supply path; Supplies cards now click-anywhere to open the editor with stopPropagation on the +/− steppers. **Lists Session 4** (`e09c7ec`): Haulout queue; `requires_haul_out` boolean added to `maintenance_tasks` and `repairs` with partial indexes; reverse-direction picker (multi-select unflagged items inside the Haulout view rather than per-card flags) keeps KeeplyApp.jsx untouched. Lists kill switch: `UPDATE user_profiles SET beta_features = array_remove(beta_features, 'lists')` reverts everyone to LandHoShell instantly. Garry's account is the only one currently flipped; other beta testers see the shell. **Open follow-ups (post-sweep):** decrement-on-completion (matching supply qty -1 when a maintenance task completes — highest-leverage Lists v2 but touches `toggleTask` which had three changes today, deferred to fresh context), equipment grouping in Supplies (~30 min, bounded), Haulout exclusions from urgency / health score / First Mate (per the principle: not actionable in water), Grocery v1 (mirrors Supplies), `supa()` helper hardening for POST+empty-body, bookmark "saved" state surviving page reload, dedup on `saved_parts` and `supplies` receive, source-derived equipment defaults in ReceiveModal.
- **5 active beta testers** (2 Active Cruisers, 2 Liveaboards, 1 Upgrader). +2 imminent (1 Cruiser, 1 Liveaboard) → 3/3/1 split.
- **Beta close deadline: May 1.** 5 testers actively working through the structured task plan; 0/5 fully complete. KR in flight.
- **Active KR: "Deliver final features" (due May 31):** Logbook Custom Checklists (Pro), First Mate Conversation History (all tiers), Multi-engine tracking. See `/admin/okr`.
- **3/3 personas validated** (all three personas represented in beta cohort)
- **iOS/Android:** DUNS in hand, Google Play account in flight, iOS build pending
- **Apr 23 session win:** Doc attach bug fixed — regular-equipment Docs tab file picker now auto-fills the label from the filename, matching the vessel-scoped tab. Closes the bug that blocked attaching files to the Jabsco bilge pump on SV Irene (and every other regular equipment card since commit `a166837`).
- **Apr 22 audit finding (still open):**
  - `push_subscriptions` table has **1 row across 10 users.** Infrastructure complete (subscribe route, cron at 13:00 UTC, VAPID keys, web-push) but enrollment flow likely broken. Needs end-to-end real-device validation.
- **Apr 21 session wins:** Tier 1 hygiene complete (error boundaries + PostHog exceptions + Prettier). Beta feedback from first personal run-through closed: onboarding urgent task, First Mate formatting, feedback copy, and biggest activation win — **email verification no longer blocks signup.** Users land in the app immediately.

---

## What's shipped

**Core product**
- Equipment cards (with docs, service logs, status)
- Maintenance tasks with urgency panels
- Repairs tracker
- Documentation (registration, insurance, renewals)
- Multi-vessel support + share vessel
- Context-aware FAB per tab
- Push notifications
- **Lists tab — 4 surfaces, live for all users (Apr 29 rebuild)** — pill router on the Lists tab: **Parts** (default landing) · **Supplies** · **Grocery** · **Haulout**. Parts reads `saved_parts` and walks rows through a needed → ordered → received lifecycle (bubble + archive icon + edit/delete sheet, undo toast on archive); receives populated from existing AI-bookmark + supply-promote flows (no FAB). Supplies/Grocery/Haulout share a generic `<SimpleListView />` (one component, three mounts via `tableName` prop) — schema contract is `{id, vessel_id, name, notes, completed_at, created_at}`. Tap-to-disappear with 3.5s undo, optimistic insert (temp row appears in <16ms via `_isTemp` flag with race protection on bubble/menu until DB reconciles). Inline "+ Add item" row at bottom of list (no FAB) — tap activates input, Enter saves and stays focused for rapid entry, Escape/× cancels. Active pill highlight is solid `var(--brand)` bg + white text + shadow (the prior `--brand-deep` resolved to medium navy on dark navy bg and was barely visible). Schema: `supplies.completed_at` added (existing 6 rows preserved, legacy `in_stock`/`min_stock`/`equipment_id`/`location_id`/`unit` columns ignored UI-side, defaulted on insert); new `grocery_items` and `haulout_items` tables with RLS via `vessel_id IN (SELECT get_my_vessel_ids())`. **The earlier scaffolding (LandHoShell + `useBetaFeature` hook + NeedToBuy/Supplies/Haulout sub-views ~3000 LOC) was killed in this session** — built to an OLD plan (three-state lifecycle, `vessel_locations` managed table, `requires_haul_out` lens, beta gate) that Garry had since simplified.
- Admin dashboard at `/admin` (whitelist-gated on `garry@keeply.boats` + user-ID allowlist in Vercel env). Sections: Goals, Engagement, Geography, Users, Revenue, Product, **Lists** (Apr 29: Parts Needed/Ordered with count + $value + distinct vessels using $1 default for unknown prices, plus Supplies/Grocery/Haulout count + vessels), System Links, Orphans, Verification.
- OKR/roadmap page at `/admin/okr` with feature backlog

**Platform**
- Supabase auth + RLS
- Static pricing config in `lib/pricing.js` — single source of truth for prices, Stripe price IDs, plan limits, storage quotas (`storageBytes`/`storageLabel`), and a `CAPABILITIES` registry for boolean feature gates (11 capabilities). Exposes `hasCapability()`, `requiredPlanFor()`, and natural-language formatters (`formatPlanSummary`, `PLAN_PROMPT_LINES`, `UPGRADE_FAQ_ANSWER`) so every user-facing plan description derives from PLANS/CAPABILITIES — no more hardcoded limit strings at call sites. Consumed by webhook, LandingPage, First Mate route/components, Support FAQ, KeeplyApp upgrade cards, LogbookPage (watch entries + passage export gates), and KeeplyApp haul-out gate.
- Stripe subscriptions, webhooks, customer portal
- Resend transactional email
- **Frictionless signup** — "Confirm email" gate OFF in Supabase (Apr 21); users land in app immediately. "Secure email change" ON for account-change protection. Branded confirmation email template ships from `Keeply <noreply@keeply.boats>` (used for explicit resend, password reset, email-change verify). Apr 28 evening: `/api/send-verification` and Supabase password-reset email templates rebranded with navy header + `"Always ready to go."` gold kicker + gold CTA, matching the rest of the brand.
- Weekly digest cron (Mon 10:00 UTC / 6 AM ET)
- PostHog, GA4, Search Console, Google Ads, Microsoft Clarity
- **PostHog exception capture** (Apr 21) — `capture_exceptions: true` + `trackException` helper wired into route + global error boundaries
- SEO: sitemap, robots.txt, JSON-LD

**Marketing site (Apr 28 architecture)**
- `components/marketing/LandingPage.tsx` (148 lines, pure presentation, zero Supabase imports) drives 8 typed section components in `components/marketing/sections/` (Hero, VideoShowcase **DELETED Apr 28 evening**, SocialProofMarquee, HowItWorks, Testimonial, CoverageChips, PricingTeaser, FinalCTA). Hero is now 2-column desktop / stacked mobile with text left + walkthrough video right.
- Coverage-first messaging across hero, FinalCTA, all CTAs, and `app/layout.tsx` metadata: H1 `"Every system. Every part. Every passage."`, FinalCTA `"Always ready to go."`, button copy `"Get Keeply Free →"` (paid tiers stay `"Get started →"`).
- AuthModal lifted to layout-level via `components/auth/AuthOpenerProvider.tsx`. Off-homepage CTAs (SiteHeader on `/about`, `/features`, etc.) call `useAuthOpener().openSignup('free')` — no nav, in-place modal. Free CTAs skip PlanPickerModal entirely (PlanPickerModal now orphaned, ~80 lines dead code in repo).

**Stability (Apr 21)**
- Route-level error boundary (`app/error.tsx`) — "Something went aground" with Retry
- Global error boundary (`app/global-error.tsx`) — catches root-layout crashes with inline dark-mode styles
- Prettier config + entire repo formatted; dead `KeeplyApp.stripped.jsx` removed

**Onboarding polish (Apr 21)**
- "Complete your vessel setup" synthetic maintenance task auto-created on signup (both free + paid AI-build paths). Lands in Due Soon urgent card. Interval 36,500 days so completion doesn't resurface.
- First Mate explicit formatting rules in system prompt — no markdown, plain dash lists, 1-2 sentence short answers, no "Great question!" preamble
- Feedback confirmation copy: *"We'll respond when we get back to the dock."* (nautical, no founder name)

**Free-tier gates finalized (Apr 25)**
- **Parts catalog removed from Free tier copy across the app.** The bullet was on the plan-picker card, the comparison table, the secondary feature strip, the support FAQ, and the `partsCatalog` capability registry — all gone. Reason: the curated browseable catalog described in marketing was never built. The actual user-facing parts feature is the per-equipment "My Parts" block, which is too modest for a marketing bullet. Honest Free tier copy beats aspirational copy that drifts. Same lesson as DOC_LIBRARY (Apr 23): aspirational copy without shipped consumers is a liability, not an asset.
- **Free tier locked at 1 vessel · 2 equipment · 3 repairs · 5 First Mate queries/mo.** No rollover (engineering-cheap but solves the wrong problem — seasonal users want pause-subscription, not banked queries; that's Layup Mode in fall 2026). 3-repair limit is intentional and counts onboarding repairs (the 2 system-created onboarding rows + 1 user repair = paywall). Garry's call against the beta task plan's earlier flag: "all they have to do is mark the first two complete." Conversion math beats UX comfort here.
- **Existing Free users above limits are auto-grandfathered.** No code change needed — `canAddEquipment(plan, currentCount)` only blocks new adds when `currentCount >= limit`, never retroactively. A user with 5 equipment cards keeps them; can't add a 6th.
- **CONTEXT.md correction:** the Apr 24 entry stating `canAddRepair()` is "defined-but-unused" is stale. As of this session it IS wired up at `KeeplyApp.jsx:4198` (`if (!canAddRepair(userPlan, repairs.length))`). Repair gate is live.

**Customer-facing BETA / early-access language removed (Apr 25)**
- Beta is wrapping; the website was still loud with "early access," "Join the beta," and `BETA2026` discount-code copy in 9 different sites of `LandingPage.jsx`. All 9 scrubbed in a single pass: top banner, social-proof "IN EARLY ACCESS" pill, pricing-section green pill + headline + subhead ("Choose the plan that fits your boat" replaces "Join the beta"), comparison-table discount-code row, "Use code BETA2026" line below the table, the full BETA2026 promo callout `<div>` block in the plan-picker modal, and the strikethrough-price + "2 months free with BETA2026" badge in the signup modal price column.
- Replacement copy originally leaned on the 14-day free trial — but the trial itself was removed Apr 25 (see "Trial removed" entry below). No new promotional language was introduced; the goal was to make the site read like a shipped product, not an ongoing beta.
- "Beta" refs left in active code are all legitimate and intentionally kept: `app/admin/okr/page.tsx` (internal OKR dashboard), `VesselSetup.jsx` Beta Marine engine comment, and the `anthropic-beta` HTTP header in the First Mate route (Anthropic prompt-caching API flag, unrelated to Keeply beta).

**Trial removed (Apr 25)**
- The 14-day free trial is gone — both the marketing callouts and the backend mechanic. `lib/pricing.js`: `trial: 14` removed from Standard and Pro. `app/api/firstmate/route.js`: the "free users with `daysSinceSignup < 14` get Pro-level First Mate access" gate is deleted; effective plan is now just `plan`. `components/KeeplyApp.jsx`: `trialActive` state, three `setTrialActive(false)` setters, three conditional checks, and four prop passes all removed. `components/FirstMate.jsx` and `FirstMateScreen.jsx`: `trialActive` prop dropped; `effectivePlan` simplified to `plan`. `components/LandingPage.jsx`: four user-visible callouts removed (top banner segment, pricing-section badge text, pricing-table footer line, signup-modal price sub-line). `app/layout.tsx`: trial removed from main meta description, Open Graph, and Twitter card.
- Reason: clean up the offer. Free tier limits (1 vessel · 2 equipment · 3 repairs · 5 First Mate/mo) are tight enough that a true conversion event happens at the limit, not at trial expiration. Trial-expiration churn is also a worse signal than limit-hit conversion.
- **Stale dev cruft NOT deleted:** `patch-pricing.js` and `patch-pricing2.js` are one-off migration scripts that originally added the trial. Already-run, no longer referenced — safe to delete in a future pass.
- **Stripe price-level trials are NOT touched here.** The four Stripe price IDs may still have a `trial_period_days` configured at the Stripe dashboard. Our `app/api/stripe/checkout/route.js` doesn't pass trial params, so subs created after this change won't trial — but if a price-level trial is configured, it'll still apply. Verify in Stripe dashboard if any subs are created with `status: 'trialing'`.
- **Admin "Trialing" stat left in place** at `app/admin/page.tsx:894` — it reads from Stripe's `subscriptions.list({ status: 'trialing' })` and is now a passive diagnostic. Should always be 0 going forward; if non-zero, something is configured at the Stripe price level.

**Pre-launch hardening (Apr 25–26)**
- **Custom email verification system** — runs alongside Supabase autoconfirm (autoconfirm stays ON, no signup gate). Three new endpoints: `app/api/send-verification/route.ts` (generates 32-char hex token, stores in `app_metadata`, emails via Resend — branded navy/gold layout as of Apr 28 evening), `app/api/verify-email/route.ts` (validates token, flips `app_metadata.email_self_verified=true`, redirects to `/?verified=1` or `/?verified=0&reason=...`), `app/api/change-email/route.ts` (admin API updates email + triggers fresh verification). UI: dismissable banner in `KeeplyApp.jsx` (trigger: `app_metadata.email_self_verified !== false`); existing users + Google OAuth exempt because their flag is undefined. `LandingPage.jsx` AWAITs send-verification then `refreshSession()` so JWT picks up app_metadata before user enters app; also handles `?verified=1`/`?verified=0&reason=...` URL params with success/error banner.
- **Admin email verification observability card** — collapsible card on `/admin` next to Orphans card. Shows `pending`, `expired`, `verified`, and `legacy/OAuth (exempt)` counts. Expanded view lists pending users with token-sent date + signup age, sorted with expired tokens first. Reuses existing delete-test-user modal for cleanup actions. Same pattern as the orphans card.
- **Admin orphan visibility** — collapsible card on `/admin` showing users with `pending_plan=standard|pro` who never completed payment. Expanded view shows email, intended plan, joined date, age. Delete action wired to the new test-user endpoint.
- **`/api/admin/delete-test-user`** — atomic Stripe + Supabase cleanup endpoint, auth-gated by `ADMIN_USER_ID` env. Used by all admin-side delete actions (orphans card, verification card). Tested end-to-end during the session.
- **Free→Standard charging bug fix** — clicking Free button left stale `localStorage.keeply_pending_price_id` from a prior Standard/Pro click, causing the Free user to hit Stripe Checkout. Fix: clear localStorage in Free button click handler.
- **Google Ads conversion tracking** — gtag added to `app/layout.tsx`, `lib/analytics.ts` (~79 lines) created with three conversions: Signup Started ($0), Plan Selected ($5), Sign Up Completed ($15, Primary). Conversion ID `AW-18080905583`; labels `a5AJCPXAv6IcEO_y0q1D`, `jsS9CPLAv6IcEO_y0q1D`, `1wl0CO_Av6IcEO_y0q1D`. Stripe purchase as 4th conversion is on the post-launch backlog. Discovered an existing GTM container `GT-NNMKZQC5` of unknown origin during this work; investigation deferred. PostHog events appearing empty in dashboard is likely the local ad blocker; not confirmed.
- **Document scan partial-fields data-loss bug fix** — scanning a registration doc then an insurance doc was wiping the registration's `hin`/`uscg_doc`/`state_reg`/`home_port`. Root cause: `scanCommitPayload` called `vesselInfoPayload(fields)` which builds the FULL passport shape with explicit nulls for absent keys — correct for the manual edit form (empty input = clear), wrong for partial scans (absent = "this doc didn't have it"). Fix: inlined passport-building logic in `scanCommitPayload` to include only keys actually present in the response. `vesselInfoPayload` itself unchanged (still correct for form-edit save path; the second scan-document caller uses `setInfoForm` to merge into form state first, which already worked).
- **`/api/delete-account` auth + scope hardening** — endpoint accepted `userId` from request body with no Bearer token verification, so anyone with a userId (which leaks via vessel_members invites, share links, etc.) could delete that account. ALSO: `vesselIds` was sourced from `vessel_members?user_id=eq.X`, which includes vessels the user is just CREW on. Cascading deletes then wiped child data for those shared vessels — destroying the OWNER's data. Both fixed: Bearer token verified via `/auth/v1/user`, rejected with 401/403 on missing/mismatched. `vesselIds` now sourced from `vessels.user_id` (owned only). `vessel_members?user_id=eq.X` DELETE moved outside the if-owned-vessels block so crew-only users still get removed from shared vessels they're on. Client (`KeeplyApp.jsx`) now reads access token from `supabase.auth.getSession()` and sends `Authorization: Bearer <token>`.

**Bug fixes & polish (Apr 23)**
- **FM_LIMITS single source of truth** — `app/api/firstmate/route.js`, `components/FirstMate.jsx`, and `components/FirstMateScreen.jsx` now all import `PLANS` from `lib/pricing.js`. The three hardcoded constants (each with different values, none matching the DB) are deleted. Standard users now see 30 in both UI surfaces and get 30 from the server, not 10. Stale 403 message ("First Mate is not available on the Free plan…") reworded to plan-agnostic and hardened with a `console.error` log so the now-unreachable branch becomes observable if it ever fires.
- **Orphan pricing schema cleanup** — dropped five unreferenced tables: `plan_limits`, `plan_marketing_features`, `pricing_plans`, `pricing_experiments`, `user_experiment_assignments`. Zero code references existed; they were seed data / A/B testing scaffolding from prior sessions that never got consumers. `lib/pricing.js` is now the unambiguous single source of truth with no parallel DB system creating drift.
- Doc attach auto-fill parity — the file picker on the regular-equipment Docs tab now auto-fills the label field from the filename, matching the vessel-scoped tab. Commit `a166837` ("Doc attach UX: auto-fill label from filename") only updated one of two file picker sites; today's one-line fix (onChange handler at line ~17017 in `KeeplyApp.jsx`) restores parity. Diagnostic logs from `4147a7d` made the diagnosis trivial — `[addCustomDoc] aborted: empty label` with a dumped form state told the whole story.
- DOC_LIBRARY removed (-335 LOC) — the "Suggested Documents" feature linked to `google.com/search?q=...` URLs dressed up as curated manufacturer manuals. Misleading; user clicks "Beta Marine Operators Manual" and gets a Google results page. Verified zero existing equipment rows had these URLs persisted (208 equipment rows checked), so no data cleanup. Replaced with a small empty-state on the Docs tab pointing users to "+ Add Document".
- Settings Danger Zone separator — Sign Out went from red to neutral text color (it's a routine action, not destructive); a "DANGER ZONE" muted label now sits between Sign Out and Delete Account. Prevents the misclick where two adjacent red buttons did very different things.
- FAB "Add Task" → "Add Maintenance" — beta users were confused by "Add Task" since the action creates a maintenance item attached to equipment. Updated the FAB sub-menu label, modal heading, and form placeholder. Tab labels (`Maintenance Tasks`) intentionally left alone — the broader task→maintenance audit stays in the icebox.

**Roughly done (remaining items tracked in "Deliver final features" KR, due May 31)**
- **Logbook** — passages, watch entries, pre-departure & arrival checklists all working. **Remaining:** Custom Checklists (Pro tier) — currently checklist items are hardcoded JS constants in `LogbookPage.jsx`.
- **First Mate** — conversational AI assistant, bottom sheet, query limits by tier (all three sites now import from `lib/pricing.js`), `APP_GUIDE` system prompt with prompt caching. **Remaining:** Conversation history (all tiers — beta tester request).
- **Engine hours (multi-engine, Apr 29)** — `engines` table is the source of truth (one row per engine, with make/model/year/HP/fuel/position/hours). Equipment cards link via `equipment.engine_id` FK. KPI strip on My Boat tab renders single/twin/triple+ variants with discrepancy pulse (>50hr abs OR >10% pct between engines). Passage form captures per-engine hours; First Mate context includes per-engine entries. `lib/engines.js` is the single source of truth for engine helpers. `vessels.engine_hours` is mirrored on writes for backwards compat. **Remaining (Phase 3, hygiene):** drop `vessels.engine_hours`, `engine_hours_date`, `fuel_burn_rate` columns + orphan `engine_makes`/`engine_models` tables. Irreversible — defer until confident no app code reads from them.

---

## Active work

Organized around two KRs pre-GoLive:

**KR: Close beta successfully (deadline May 1)**
- **Beta tester activation** — structured task plan still 0/5. Signup friction is gone; send re-engagement: "Hey, verification is now instant — try again."
- **Push notifications validated end-to-end** — diagnose why `push_subscriptions` has only 1 row across 10 users. Test real-device delivery on the 13:00 UTC cron.

**KR: Deliver final features (due May 31)**
- ~~**Custom Checklists**~~ — ✅ Shipped Apr 24 (narrow scope, all tiers). New `vessel_checklist_items` table; Pre-Departure and Arrival tabs get an "Edit" button that lets users add/remove/reorder/rename items. First edit seeds from the hardcoded defaults and replaces them; "Reset to defaults" deletes custom rows and falls back. Hardcoded sail-only flag and category grouping NOT ported to custom items — simpler edit UI, users can name/prefix items as they like. Full version (arbitrary new checklists with trigger binding) moved to post-launch backlog in ROADMAP.md.
- **First Mate Conversation History** — new `first_mate_messages` table (user_id, vessel_id, role, content, created_at). Single rolling thread per vessel, free for all tiers, context-window cap at ~30 messages. Beta-user request.
- ~~**Multi-engine tracking**~~ — ✅ Shipped Apr 29 (Phases 2A–2G). New `engines` table is source of truth; `equipment.engine_id` FK links equipment cards to engines; `passage_engine_hours` table for per-engine passage logging; `lib/engines.js` is single source of truth for helpers. KPI strip on My Boat tab handles single/twin/triple+ with discrepancy pulse. Backfill banner exists for 6 vessels missing engine identity (incl. Irene). **Phase 3 (legacy column drop) deferred** as low-priority hygiene — see "Roughly done" section.

**Other in-flight (not in above KRs)**
- **Google Play** account verification + 12-tester closed testing setup
- **iOS** build prep

### Post-session pending (Apr 21)

Queued but NOT urgent. Order by discretion:

- **Env vars to mark Sensitive in Vercel** (in priority order, each requires copying current value to password manager first since you can't view after upgrade):
  1. `ANTHROPIC_API_KEY` (biggest blast radius)
  2. `RESEND_API_KEY`
  3. `SIGNUP_WEBHOOK_SECRET`
  4. `CRON_SECRET`
  5. `VAPID_PRIVATE_KEY`
  - Skip `VAPID_PUBLIC_KEY` — public keys stay public, false-positive flag.
- **VS Code format-on-save** — install `esbenp.prettier-vscode` + add `.vscode/settings.json` with `formatOnSave: true`. 30 seconds.
- **Tier 2 code hygiene** — Husky pre-commit hook (~30 min), Playwright smoke tests for 5 critical paths (~3–4 hrs), `/api/invite` rate limit (~30 min), `/api/stripe/checkout` JWT verification (~30 min). ~5 hrs total, spread across sessions.

### Post-session pending (Apr 25–26)

All low-priority cleanup; nothing here blocks GoLive.

- **Apr 26 late PM additions:**
  - ~~**Messaging audit (HIGH).** 5 root-metadata strings in `app/layout.tsx` still carry old "AI-powered" / "Your vessel's First Mate, always ready" framing~~ — **CLOSED Apr 28 evening.** All root metadata flipped to coverage-first in the same pass that propagated "Get Keeply Free" globally.
  - **First Mate silent-bail UX patch.** `send()` in `FirstMateScreen.jsx` L374 silently bails on `!q || loading || !context || isAtLimit`. When context fails to load (network blip, DevTools throttle, etc.) suggested-question chip clicks do nothing visible. ~30 lines: disable chips while context loading, inline "Loading your boat's data...", `console.warn` on bail-out. Filed in BACKLOG.
  - **Unify dual Supabase clients.** `components/supabase-client.js` (hardcoded URL/key, used by KeeplyApp) AND `lib/supabase.js` (env-var-based, used elsewhere) both call `createClient()` separately, producing the visible "Multiple GoTrueClient instances detected" console warning. Latent token-refresh-race risk. Need a focused unification pass — pick env-var pattern (lib/supabase.js), migrate KeeplyApp + supa() helper + all imports, delete the hardcoded one. Estimated 1-2 hours.
  - **Software Apps schema `aggregateRating`.** Google Rich Results Test flagged it as missing-but-non-critical. Deliberately deferred until App Store reviews exist (≥25 real reviews from non-friends). Self-published ratings before then are spam-flag risk to the entire domain.
  - **3-phone Hero #2 follow-up:** when SV IRENE walkthrough video gets recorded, swap each `PhoneScreenshot` for a `<video>` element. Pattern is one-line per phone. *(Partial: Apr 28 morning replaced the static phone with a walkthrough video; Hero #2's three-phone variant is no longer the active hero design.)*
  - **Feature page tone consistency:** /features still has First Mate-prominent copy in the closer section; may need lighter-touch treatment for tone consistency now that About has scrubbed the AI-first framing. Audit after messaging-audit task lands.
  - **PricingClient AI-first language audit.** Quick scrub for any remaining "AI-first vessel intelligence" / "Your vessel's First Mate" strings.
  - **Filename hygiene:** `public/images/spinnaker.jpg` is actually a sunset shot, not a spinnaker. Rename to `sunset-passage.jpg` and update one ref in AboutClient.tsx. 30 seconds.
- **Token cleanup bug in `/api/verify-email`** — `delete newAppMeta.verify_token` doesn't actually clear the key when sent through `updateUserById` (Supabase merges, doesn't replace). Cosmetic only — `email_self_verified=true` short-circuits everything else. 2-line fix: change `delete newAppMeta.verify_token` and `delete newAppMeta.verify_token_expires` to assignment-to-null instead.
- **Stale verification email UX** — clicking a verify link from a deleted/expired test account redirects to `/?verified=0&reason=notfound`. The LandingPage banner explains the failure but the copy could be friendlier ("This link is from a deleted or expired test account").
- **Orphan-row cleanup on user delete** — `firstmate_usage`, `push_subscriptions`, `affiliate_clicks` rows aren't cleaned up when a user is deleted. Harmless (no foreign-key constraints on `auth.users` so nothing breaks); accumulates as cleanup debt over time.
- **Stripe purchase as 4th Google Ads conversion** — currently the Sign Up Completed event fires for ALL signups including Free. A separate Purchase event for paid checkouts would tighten conversion attribution.
- **PostHog silent issue investigation** — events appear empty in dashboard. Likely Garry's own ad blocker (never confirmed). Worth checking from an unblocked browser session.
- **Existing GTM container `GT-NNMKZQC5`** — discovered during ads tracking work; source unknown. Audit and decide whether to keep or remove.
- **"Check your inbox" modal copy on LandingPage** — copy says "click to activate" but autoconfirm is ON now, so users land in the app immediately. Modal copy is misleading; minor update needed.
- **Notification toggles cleanup (Apr 26)** — Settings → Alert Notifications had three rows: In-app alerts, Email digest, Push notifications. Audit found: In-app alerts was fully aspirational (saved to user_metadata but no bell icon ever shipped, nothing reads the flag). Email digest was wired to weekly cron but mislabeled "Daily summary". Push notifications fully wired. Patch shipped: removed In-app alerts row, fixed Daily→Weekly label. `alertInApp` field still loads/saves in user_metadata so the bell can be added later without losing existing user prefs. Bell icon build added to ROADMAP.md post-GoLive backlog item #17. Granular timing controls (alertOverdue/alert3day/alert7day/alertDayOf/digestTime) still saved but ignored by cron — backlog item #18.

### Post-session pending (Apr 28)

Cleanup debt accumulated this evening's session — file when Garry calls a hygiene pass:

- **`PlanPickerModal.tsx`** — orphaned file, no longer referenced anywhere. Free CTAs skip it; paid-tier flows go through `/pricing`. ~150 lines.
- **`?plans=1` URL param parsing** in `useAuthRedirects.ts` — orphaned, still parsed defensively. `showPlanPickerOnMount` field in `AuthRedirectsResult` is read by nothing.
- Total ~80 lines dead code across the three sites.
- **Hero #2 three-phone pivot followups** — `hero-firstmate.jpg`, `hero-logbook.jpg` may now be unused if the active Hero shows only the walkthrough video. Worth grepping.
- **`hero-poster.jpg` / `hero-poster-mobile.jpg` in `public/images/`** — committed to repo but referenced nowhere now that the preload tags are gone (Apr 28 morning). ~340 KB repo bloat. Already filed in Apr 28 morning entry's "Open / next session priorities."
- **Magic Link / Confirm Signup / Change Email Address Supabase email templates** — defer from Apr 28 evening session. Branded password reset + verification done; these three remain on default Supabase chrome.

### Post-session pending (Apr 29)

Tech debt accumulated during the multi-engine + Lists rebuild + admin metrics push. None block GoLive.

- **Lists shared primitives extraction.** `UndoToast`, `ActionSheet`, `EditSheet`, `Field`, `SheetButton` are duplicated (~150 LOC) across `components/Lists/PartsView.jsx` and `components/Lists/SimpleListView.jsx`. Extract to `components/Lists/shared.jsx` and have both views import from there. Code drift risk if either gets updated independently. ~1 hour focused work.
- **Engine identity backfill banner — re-expose as permanent affordance.** Phase 2G shipped a banner + bottom-sheet modal (~445 LOC) on My Boat tab targeting only 6 vessels (incl. Irene) that need `make`/`model` filled on auto-backfilled engine rows. Once those 6 backfill, the banner code is dead. The bottom-sheet edit modal itself is good UX — re-expose it as a permanent "Edit details" affordance on engine equipment cards (pencil/lock icon that opens this same editor) so the modal lives on after the banner dies.
- **Phase 3 multi-engine cleanup.** Drop legacy `vessels.engine_hours`, `vessels.engine_hours_date`, `vessels.fuel_burn_rate` columns; drop orphan `engine_makes` (21 rows) + `engine_models` (271 rows) tables. **Irreversible.** Sweep the codebase for `vessels.engine_hours` reads first — confirm zero remain — then run the migration. Low priority; current dual-write keeps the legacy columns useful as a safety net.
- **`saveLog` dead code in `KeeplyApp.jsx`** at line ~5520 — discovered during Phase 2D scoping, never wired to anything. Cleanup in a future hygiene pass.
- **MAX(hours_end) correctness fix** for engines reading from `passage_engine_hours` — when computing "current engine hours" we should use the max across passages, not just the most recent passage's value (in case passages are logged out of order). Filed in ROADMAP backlog item 14.
- **Lists empty-state copy pass** after Garry has used all 4 surfaces in real workflows for a few days — current copy is best-guess.
- **Optional: FAB on Parts surface** if users (or Garry) want to manually add parts without going through AI-bookmark flows. v1 skipped this deliberately to keep Parts identity narrow.

**~~Moved to icebox (Apr 22)~~ — superseded Apr 25–26.** Original Apr 22 decision was to ice the unverified-email banner because `.resend({type:'signup'})` is a no-op on auto-confirmed users. That decision was reversed Apr 25–26 by building a custom verification system that runs alongside autoconfirm: `app_metadata.email_self_verified` flag, custom token in `app_metadata`, banner trigger reads the flag, three new endpoints, all detailed in the "Pre-launch hardening" block above. Autoconfirm stays ON (no signup gate); the custom system layered on top is what delivers the safety property without the friction.

---

## Code hygiene plan

**Current state (Apr 21, 2026):** Tier 1 complete. ESLint v9 + TypeScript v5 installed. Prettier configured and entire repo formatted. Route + global error boundaries shipped. PostHog exception capture wired up. **Still missing:** tests (zero), pre-commit hooks. File mix: 34 `.js/.jsx` (including the ~26k-line monolith — Prettier reformat, ~8k substantive) vs 18 `.ts/.tsx`.

### Tier 1 — this week (~3 hrs)
1. **Error boundary at app root.** Prevents white-screen-of-death from any uncaught React error. ✅ Shipped Apr 21.
2. **PostHog error tracking** (not Sentry — chose PostHog because it's already installed; one fewer service to manage; switching cost is low if insufficient). `capture_exceptions: true` + `trackException` helper wired into both error boundaries. ✅ Shipped Apr 21.
3. **Prettier + format-on-save.** Kills style drift, makes diffs clean. ✅ Shipped Apr 21 (also removed dead `KeeplyApp.stripped.jsx` file that was caught by the format pass).

### Tier 2 — next 2 weeks (~5 hrs)
4. **Husky + lint-staged pre-commit.** Solo-founder substitute for PR review; blocks unformatted/ESLint-failing commits.
5. **Playwright smoke tests — 5 critical paths.** Signup → lands in app, add vessel, add equipment + mark maintenance done, Stripe subscribe, First Mate query. One test per path is enough.
6. **`/api/invite` rate limit.** Route currently has no auth; anyone knowing the endpoint can blast Keeply-branded emails. Verify Bearer token, reject if caller has sent >5 invites in the past hour (Supabase count query on `vessel_members` by inviter). Two-line check. Closes the spam-vector hole without needing verification plumbing. ~30 min.
7. **`/api/stripe/checkout` JWT verification.** Route trusts client-sent `userId`. Add `supabase.auth.getUser(token)` check, reject if `userId !== token.sub`. Prevents a logged-in user from starting a checkout attributed to another user. ~30 min.

### Tier 3 — post-launch ONLY
- Split `KeeplyApp.jsx` into sub-components
- Convert remaining `.jsx` → `.tsx`
- Zod schemas at API boundaries
- Component-level unit tests
- Full theme audit / design tokens / CSS variables

### Deliberately NOT doing
- CI/CD beyond Vercel built-in (it's enough)
- Monorepo tooling (no need at this scale)
- TypeScript strict mode blitz (gradual adoption on new files only)
- Unit tests for every component (ROI too low for 1-person team)
- Custom test infrastructure beyond Playwright defaults

---

## First Mate build order (remaining)

_Engine hours foundation is shipped; multi-engine is in the active "Deliver final features" KR. List below is post-GoLive._

Ordered by strategic value:

1. **Consumables on equipment cards** (pre-populated at onboarding; pairs with engine hours for dual-trigger)
2. **Text First Mate**
3. **Weather API** — NOAA/Open-Meteo free tier (Windy/PredictWind partnership is Phase 2)
4. **Provisioning & par system**
5. **Voice input** for First Mate
6. **Departure Check enrichment** (voice + weather on existing checklist)
7. **Voice output**

---

## Icebox

- **Camera equipment ID** — Take a photo of an engine plate/equipment label → AI identifies make/model. Genuinely differentiated (no competitor does this); net-new work, not a prompt tweak. Requires new API route or extension of `scan-document`. Added to OKR backlog Apr 21. **Note:** current AI equipment ID is text-only via `identify-vessel` route. `scan-document` exists but only handles registration documents, not equipment.
- Quick Capture (photo → AI ID → one-time or recurring) — depends on Camera equipment ID above
- Windy partnership (strategic, co-marketing)
- Vessel notes freeform field (marina slip, contacts, radio channels)
- Terminology audit: "task" → "maintenance" throughout app UI (website already done)
- Offline-first logbook for offshore use
- Native mobile shells (vs. PWA/TWA path)
- Insurance sponsorship model (BoatUS/Geico) — requires consumer scale first

### Away Mode / Layup Mode (design parked)

**Status:** Icebox. Re-evaluate fall 2026 when PNW/Northeast users approach layup season.
**Why parked:** Current April beta cohort has no one in a layup-relevant state (Florida doesn't layup; PNW boats are commissioning, not decommissioning).

**Design direction when we pick it up:**

- **Name it "Layup Mode," not "Away Mode."** Boaters already understand "layup" — specific framing prevents scope creep into vacation/storm/sale cases.
- **Option C with simplicity of A.** Layup Mode with winterization checklist on entry and spring commissioning checklist on wake-up. Checklist is skippable — user can just pick a return date for speed.
- **Repairs stay live.** Maintenance gets deferred; repairs don't. A broken through-hull doesn't care that you're away.
- **Show muted, don't hide.** Tasks dim/collapse under an "In Layup — paused items" header. Hiding them creates data-loss anxiety.
- **Health score shows "🛌 Vessel in Layup — resume [date]" explicitly.** Don't silently freeze the score; silent freezing looks like a bug.

---

## Key learnings (Apr 28 evening session)

- **Stable callbacks > stable objects when they go in dep arrays.** The "modal close + login stuck" bug came from putting a memoized context value (`useAuthOpener()` return) in a useEffect dep array. The provider had `useMemo` keyed on `[isOpen, isRecovery, ...callbacks]`, so the value identity changed on every modal toggle — but the callbacks inside it (`useCallback([])`) were stable across renders. Effects that depend on the WHOLE provider value re-fire on every toggle; effects that depend on the destructured CALLBACKS only re-fire when the callbacks themselves change (i.e. never, here). Pattern lesson: when consuming a context, destructure the stable parts and depend on those individually. Don't put the whole hook return in `[]`.
- **A "no-blink" architecture has a tradeoff: post-signup blink replaces pre-signup blink.** Lifting AuthModal to layout level eliminates the SiteHeader-on-/about → nav to home → flash → modal sequence. But because the provider hard-navs to `/` after a successful signup-from-/about, the user briefly sees the marketing flash on the way to KeeplyApp. Net: pre-signup felt broken (rejection signal); post-signup feels like loading (success signal). Garry took the trade. Worth documenting because the "no-blink" framing oversimplifies.
- **Procedure violations should self-flag, not work around.** Mid-session my filesystem tools (`create_file`, `bash_tool`, `view`, `str_replace`, `present_files`) dropped, leaving only Chrome browser. Instead of stopping the session and naming the limitation, I pasted code in chat — forcing Garry to assemble files manually. Garry called it out (correctly): a working contract is more valuable than continuing the work in a broken way. New rule (now lifted to Operating rules → Session-start ritual): if tools drop, flag it, stop the deploy flow, wait for tools or pause the session. Don't substitute a worse delivery method.
- **The "Get Keeply Free" CTA is doing two jobs at once.** It signals (a) low commitment ("free tier exists, click to try") and (b) skip-the-picker behavior ("don't show a plan chooser, just sign me up"). Both jobs are real. If the marketing asks down the road for "show plan picker as an explicit funnel step" — for instance for a Pro pricing experiment — the CTA copy and the click handler need to move together. Right now they're conceptually coupled but technically separate (button text in N components, behavior in M handlers). One PR could break the alignment.

## Key learnings (Apr 26 PM session)

- **Reverse-direction picker beats per-card flag for low-frequency curation.** First instinct for the Haulout queue was to add a `requires_haul_out` checkbox to every maintenance task editor and repair editor — touching `KeeplyApp.jsx` in two places, cluttering every card with a checkbox most users will never tick. Inverted: the flagging UI lives inside the Haulout view itself ("+ Add items" → multi-select all unflagged tasks/repairs at once), zero changes to KeeplyApp. Matches how users actually think — "what should I do at next haulout?" is a planning moment, not a per-task moment. Same pattern applies to any future binary flag where the read view is meaningful but the write event is rare and batched (Layup Mode, possibly Spring Commissioning Mode if it ever ships).
- **A single helper bug becomes a permission slip to patch every call site.** The `supa()` POST helper chokes on `prefer: return=minimal` because PostgREST returns HTTP 201 with empty body and `res.json()` throws "Unexpected end of JSON input." Today's fix dropped the prefer header at the call site (`0f49abe`). Right call for the moment — narrow blast radius, ship the bookmark feature — but every future POST that uses the helper carries the same bug and the same workaround. The right fix is in the helper: `const text = await res.text(); return text ? JSON.parse(text) : null`. Filed in open follow-ups; promote when the second call site hits this.
- **Three changes to one function in one session is the noise floor; a fourth needs fresh context.** `toggleTask` was modified three times today: service log photo snapshot, active-photos clear, Mark-as-Done sheet redesign. Decrement-on-completion (the highest-leverage Lists v2 feature) would have been a fourth touch, in the same session, with no test coverage of the prior three. Vetoed in favor of fresh tomorrow context. The rule: if the diff to a single hot function exceeds 2-3 surgical changes within one session, the next change wants a clean read of the file.
- **"Code and docs deploy together" doesn't mean per-commit.** The hard rule says code and docs ship in the same logical unit. The interpretation today: docs sweep runs ONCE at end of session, capturing all 14 deploys in a single CONTEXT.md + OKR commit. Mid-session doc updates would have meant rewriting the same paragraph 5 times as Lists Sessions 2/3/3.1/3.2/4 evolved. Better signal-to-noise for future-Claude reading the diff.
- **Claude built CONTEXT.md staleness into its own start ritual and then kept building features instead of updating docs.** The session-start memory says read CONTEXT.md first. By Lists Session 3, CONTEXT.md was 11 deploys behind reality. If today's session had crashed at any point and tomorrow's Claude pulled the repo, it would have read a CONTEXT.md from before any Lists work and made decisions assuming the founder-only beta gate didn't exist. Mitigation in this case: human noticed and called the sweep. Better mitigation: at end-of-session, even when momentum says ship one more feature, the sweep is the higher-leverage move because it preserves continuity for every subsequent session.
- **`SELECT DISTINCT` shows what exists, not what's allowed.** Lists Session 5 added `source_type='supply'` to the `saved_parts` table. The pre-build verification ran `SELECT DISTINCT source_type FROM saved_parts` and saw `'task'` and `'repair'` — concluded the column was free-text and shipped. Production rejected every `'supply'` insert via the `saved_parts_source_type_check` CHECK constraint that was never queried. The `console.error` in the helper caught the rejection but produced no user-visible feedback, so the bug looked like "nothing happens." Caught only when Garry reported the symptom. **Rule for next time:** when adding a value to any text column that smells domain-like (status, kind, type, role, state), `SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid='public.<table>'::regclass AND contype='c'` before assuming.
- **Silent insert failures need a user-visible signal.** The Session 5 helper followed the pattern used elsewhere in the codebase: `if (insErr) console.error(...)`. That's fine when the user is expected to open devtools, but useless for a beta tester. The bug only surfaced because Garry compared "I dropped 5 supplies to zero" to "Need to buy is empty" and ran the math. For helpers that fire on user actions and create user-visible artifacts, errors should at minimum show a toast — even a generic "Couldn't update list, see console for details." Filed under hygiene; worth a pass when the smoke tests go in.
- **Out-of-stock should always queue regardless of threshold.** Initial helper rule was "below `min_stock` triggers" — but a supply with `min_stock=0` (the default) and `in_stock=0` would never trigger. Out-of-stock is unambiguously "need more" regardless of whether the user set a threshold. Patched: `in_stock=0` is a universal trigger condition; `min_stock>0 AND in_stock<=min_stock` is the secondary trigger. The two conditions are OR-ed in the helper. Lesson on default values: when the threshold IS the trigger condition, the default of 0 means "no trigger" — but for "I need more" semantics, zero is universally meaningful and the default needs special-case handling.

## Key learnings (Apr 25–26 session)

- **Two functions, two write paths, two different rules.** `vesselInfoPayload` builds a FULL-shape vessel passport object with explicit nulls for absent keys — correct for the manual edit form, where an empty input means "clear this field." `scanCommitPayload` was calling it for partial scan results, where an absent key means "this document didn't contain that field." Same data shape, opposite semantics. The fix wasn't to change `vesselInfoPayload` (it's right for the form path); it was to inline a partial-builder in `scanCommitPayload`. Lesson: when two callers want different null-handling behavior, they need different builders. Don't refactor toward a "general" helper unless its callers actually want the same semantics.
- **"15-minute auth fix" was actually a 45-minute auth + scope fix once you read the file.** `/api/delete-account` had two distinct vulnerabilities: missing Bearer token verification (the security issue Garry asked about) AND over-broad cascading deletes scoped to `vessel_members` instead of `vessels.user_id` (a data-loss issue I'd noted earlier in the session but hadn't tracked). With 3 active member relationships in production, the data-loss path was a live risk, not theoretical. Lesson: when you're already inside a security-critical file, audit the WHOLE flow, not just the field the user pointed at. Bundling both fixes into one commit also ensured the new auth check actually protected against a meaningful attack — without the scope fix, the auth check would let a properly-authenticated crew member still wipe an owner's data via self-delete.
- **CASCADE relationships eliminate manual delete chains.** Schema audit showed most vessel-scoped tables (`engines`, `equipment`, `logbook`, `maintenance_tasks`, `repairs`, `vessel_admin_tasks`, `vessel_checklist_items`, `vessel_members`, `watch_entries`) have `ON DELETE CASCADE` on `vessel_id`. The existing manual deletes in `/api/delete-account` are belt-and-suspenders against the cascade, not strictly required. `service_logs` doesn't even exist anymore — the DELETE on it has been a no-op for unknown duration. Kept the manual deletes in place this pass (low risk, idempotent) but a future cleanup pass can lean entirely on cascades. Schema-first thinking beats endpoint-by-endpoint cleanup discipline.
- **Custom verification on top of autoconfirm beats flipping autoconfirm.** Apr 22 ran into Supabase's "Confirm email" toggle disabling both the gate AND the email send. Apr 25–26 went a different direction: keep autoconfirm ON (no signup friction, no `.resend({type:'signup'})` dependency), add an `app_metadata.email_self_verified` flag set by our own endpoint, banner triggers off the flag. Autoconfirm + custom flag = best of both: zero signup friction, full observability, no Supabase dashboard couples. Trade-off: two systems to reason about (a "verified" auth user can have `email_self_verified: false`); the comment in `KeeplyApp.jsx` makes this explicit. **Not** a long-term sustainable pattern if more verification dimensions appear (phone, identity), but right-sized for the actual need.
- **`app_metadata` vs `user_metadata` — clients can write to user_metadata.** `app_metadata` is server-controlled (only writable via service role / admin API); `user_metadata` is editable by the authenticated user. Verification flag MUST live in `app_metadata`, otherwise a malicious user could flip their own flag from the client. Subtle Supabase distinction worth keeping in mind for any future trust-bearing flag.
- **`updateUserById` merges metadata, doesn't replace.** When clearing a token via `delete newAppMeta.verify_token`, the property doesn't actually disappear from the row because Supabase merges the partial object instead of replacing. To clear, set the value to `null` instead. Functionally harmless in this case (`email_self_verified=true` short-circuits any verify-token check), but a 2-line follow-up fix to clean up cosmetically. Same gotcha applies to any future metadata-clearing logic.
- **Aspirational copy is technical debt.** April had two cases where copy promised features that weren't shipped: parts catalog (Apr 25, removed across 5 sites) and DOC_LIBRARY (Apr 23, removed). Today's add was the inverse direction — `keeply-ai-usage.docx` reference document. The act of writing it surfaced an honest accounting of where AI works (First Mate, identify-vessel, scan-document) and where it's weak (older/foreign equipment, glare on phone-camera scans, multi-page PDFs). Documents written for INTERNAL reference can be more honest than copy written for external readers; they should be — that's their job.

## Key learnings (Apr 24 session)

- **The Apr 23 FM_LIMITS fix killed the symptom, not the class.** Apr 23 migrated three FirstMate enforcement constants into `lib/pricing.js`, correctly fixing Standard users being cut off at 10 queries while their UI showed 30. But the same numbers also lived as hardcoded text inside the First Mate system prompt (`APP_GUIDE` in `app/api/firstmate/route.js`) and the public support FAQ — neither touched by the constant unification. A Free-tier user asking First Mate *"why can't I add more repairs?"* got back an answer citing a 3-repair limit that does not exist in code. Aspirational text drifts faster than code because it has no compiler.
- **The fix for this class of bug is a rule, not a patch.** Added a `CAPABILITIES` registry, storage as first-class fields, and derived constants (`PLAN_PROMPT_LINES`, `UPGRADE_FAQ_ANSWER`, `formatPlanSummary`). File header now asserts: *"every natural-language description of a plan must come from a helper in this file."* Future plan changes surface any remaining hardcoded strings automatically — wrong numbers become immediately visible the moment a limit shifts.
- **Numeric limits and boolean gates are different axes; one schema was fighting both.** PLANS previously carried both vessel counts and feature flags (inside `features: []` arrays). Splitting boolean gating into a separate `CAPABILITIES` object means the pricing comparison table on LandingPage can render directly from the registry, and runtime gates use `hasCapability(plan, key)` instead of hardcoded `plan === 'pro'` checks scattered across the monolith. Adding a new gated feature is now a 1-line registry entry plus a 1-line call-site check.
- **Scope discipline on the refactor.** 12 call sites reference plan data across the codebase. 5 had drift-bug-level problems; 7 are currently correct but hardcoded (FAQ page, LandingPage pricing-table rows, LandingPage feature bullets). Phase 1 migrated only the 5 broken sites — the other 7 stay on a Phase 1b backlog, to migrate organically when the next plan change surfaces drift. Smaller deploy, smaller blast radius, cheaper rollback.
- **`canAddRepair()` has been defined-but-unused since Apr 22.** Grep confirmed zero imports anywhere. The repair paywall the beta task plan warned about does not exist — no code enforces a repair limit on any tier. Left the function in place because Garry has a repair gate planned; flipping `repairs: 3` on Free plus one `canAddRepair()` call site in KeeplyApp is now the complete enforcement path (Phase 2, separate deploy).
- **Cross-component DB writes need explicit state-sync callbacks.** The engine-hours KPI on My Boat went stale when a passage's `hours_end` was edited inside LogbookPage. KeeplyApp's own internal passage save updates both DB and React state (`setVessels(...)`); LogbookPage's save only updated the DB because it had no way to push back. Fix: pass an `onEngineHoursUpdate(hours, dateStr)` prop callback from KeeplyApp into LogbookPage; LogbookPage calls it after a successful vessels-table write. **Lesson:** any sub-component that writes to a table the parent is already holding in state needs a callback contract — DB is the source of truth, but React state is what the user sees, and "last write wins to the DB" doesn't propagate to siblings without explicit wiring. Watch for this same pattern with future sub-components (Repairs, Equipment, MaintenanceTab when those get extracted from KeeplyApp post-launch).
- **Engine hours auto-update is "latest write wins" regardless of passage chronology.** Editing an old passage's `hours_end` overwrites `vessels.engine_hours` with that older value, even when newer passages exist with higher numbers. Correct behavior: `vessels.engine_hours` should be `MAX(hours_end)` across all passages. Not fixed today — the visual bug was the user-reported issue and got resolved with the state-sync callback. Real-data correctness fix added to backlog (Phase 2c-followup) — ~10-15 min, requires changing the auto-update to compute MAX rather than blindly assign.
- **Gesture UX is a high-cost detour pre-launch.** Spent two iterations chasing a click-to-move and then a long-press-anywhere reorder for the checklist editor before reverting to plain up/down arrows. Both attempts had subtle event-handling bugs (input capture, propagation, drag thresholds) that made the feature unusable on mobile. Lesson: for low-frequency interactions (reorder happens occasionally, not on the hot path), pick the boring solution first. Arrows with adequate touch targets (28px min) are not pretty, but they work on every device immediately. Save the gesture-pattern reach for things users actually do hundreds of times.

## Key learnings (Apr 23 session)

- **Orphan schema creates recurring forensic cost.** CONTEXT.md claimed "Database-driven pricing (live tuning via `/admin/pricing`, no redeploy)" — but zero code referenced the `plan_limits` / `pricing_plans` / `plan_marketing_features` tables, and `/admin/pricing` didn't exist. 15 minutes of the session went to reconciling stated state vs. actual state. The right fix wasn't to build the missing consumers — it was to delete the unused tables so CONTEXT.md could tell the truth in one line. Rule: infrastructure without a consumer is a liability, not an asset. At 14 users, speculative schema is pure forensic tax on every future session.
- **Also: aspirational docs rot faster than code.** `lib/pricing.js` has declared itself "SINGLE SOURCE OF TRUTH" in its header for months, yet three FirstMate files each hardcoded their own version of the same data. The rule was written; the rule wasn't enforced. If a file's header asserts a cross-file invariant, add a grep check to CI or to the deploy PowerShell — otherwise the invariant will drift.
- **Verify handoff specs before executing them.** A prior session produced `keeply-docs-photos-rebuild.md` — a 900-line surgical-edit spec whose opening paragraph stated a DB migration was already applied. It wasn't: the `documents` and `photos` tables don't exist, and the old JSONB columns (`equipment.docs`, `maintenance_tasks.photos`, `maintenance_tasks.attachments`, `repairs.photos`) are all still present. Running Phase 1 of that spec would have removed working code to match a non-existent schema. Preflight (one SQL query + one grep) caught it in under a minute. Spec has been deleted from project knowledge; it was never safe to execute as written.
- **Diagnostic logs pay for themselves.** The `[addCustomDoc] aborted: empty label` log line with a dumped form state told the whole story of today's bug. Commit `4147a7d` ("replace silent returns with visible errors + diagnostic logs") was the exact tool that made the fix trivial. Next time there's a temptation to remove verbose logging from a sensitive flow, don't.
- **Duplicated UI patterns will drift.** Two file-picker sites, same `newDocForm` state, one updated with auto-fill in commit `a166837` and the other missed. Monolithic `KeeplyApp.jsx` makes this pattern inevitable until the post-launch component split. For now: whenever a UX change touches a pattern that may appear twice in a single state object, grep the file for all callers before committing.
- **Equipment RLS overlap was real but not today's bug.** The `equipment` table carries two ALL-command policies — one owner-only (`vessels.user_id = auth.uid()`), one member-aware (`get_my_vessel_ids()`). For owners on their own vessels both pass; for shared members with certain access patterns UPDATE can silently fail. Already on the backlog as "Finalize Share Vessel Permissions." Doc-attach debugging surfaced it, didn't cause it.
- **The actual user-visible symptom ≠ the root cause in the last Claude's head.** The prior session went to a full JSONB→normalized-tables refactor when the bug was a single missing line in one of two onChange handlers. Resist the urge to rewrite architecture when a bug can be explained by drift between two sites of the same pattern.

---

## Key learnings (Apr 22 session)

- **Supabase auto-confirm breaks `.resend({type:'signup'})`.** Verified empirically: with "Confirm email" toggle OFF, signups return `email_confirmed_at = created_at` instantly and `.resend({type:'signup'})` returns 200 but is a silent no-op (confirmation_sent_at stays null, zero audit events). Any future verification-flow design has to either un-confirm users post-signup via service role, roll its own verification column, or move to OAuth-primary. Don't trust Supabase's toggle semantics without an empirical check.
- **Verify a feature is still needed before building around a constraint.** Spent a chunk of the session designing a banner for a risk (chargeback fraud, invite spam, digest cost) that at 14 users is theoretical. The 30-minute mitigation — rate-limit `/api/invite` + verify auth token on `/api/stripe/checkout` — covers the actual surface without the architectural cost of a verification system. Scope question beats design question.
- **Constants drift: `FM_LIMITS` hardcoded in 3 places with 3 values.** API route (`route.js`) enforced `free: 3 / standard: 10 / pro: 50`, `FirstMate.jsx` displayed `5/30/50`, `FirstMateScreen.jsx` displayed `3/10/50`. None read from the `plan_limits` table. A Standard user saw "30 queries/mo" in UI but got cut off at 10 by the server. Correctness bug that explained mixed beta feedback. Shipped fix Apr 23: all three sites now import from `lib/pricing.js`; orphan `plan_limits`/`pricing_plans`/`plan_marketing_features`/`pricing_experiments`/`user_experiment_assignments` tables dropped. Lesson: DB-driven pricing isn't real just because a table exists — and at this scale, "static config in a lib file" beats "half-built DB-driven system with no admin UI." The right fix for speculative infrastructure is often deletion, not completion.
- **Infrastructure built ≠ infrastructure working.** Push notifications have subscribe route, unsubscribe route, VAPID keys, web-push lib, and a daily cron — but `push_subscriptions` has 1 row across 10 users. Code shipping and users being served are separate things. Always verify end-to-end on a real device before marking "done."
- **Engine hours is already done for single-engine.** `vessels.engine_hours` + `engine_hours_date` columns, logbook passages auto-update them, KPI card renders them. The actual work in the new KR is *multi-engine extension*, not "add engine hours from scratch." Grepping the codebase surfaced this in under a minute.
- **Prettier reformat changed the monolith from 7,900 → 26,180 lines without adding features.** CONTEXT.md has been repeating the old number. Line count is a poor proxy for complexity after a mechanical reformat. Substantive code is closer to 8k.
- **Backlog items in an active KR should exit the backlog.** Having Logbook, First Mate, and Engine hours in both the KR and the Feature Backlog table created implicit duplication. Rule going forward: once something enters an active KR, remove it from the backlog table; re-add if de-scoped.
- **Repo root has accidental artifacts.** `ersgarrykeeply` (no extension, 10KB), `patch-pricing.js`, `patch-pricing2.js`, `strip-parts.py`, and `.bak` files are all one-shot tools living in main. Non-urgent cleanup.

---

## Key learnings (Apr 21 session)

- **Vercel dashboard "preview" iframe is unreliable** — every Vercel deploy sends `x-frame-options: DENY`, which makes the embedded preview panel flash white. Always test deploys via the direct staging URL (`keeply-git-staging-garry-cmds-projects.vercel.app`), NOT the Vercel dashboard preview.
- **Env vars must be set per environment.** `SUPABASE_SERVICE_ROLE_KEY` was set for Production only, not Preview. First Mate on staging crashed with "supabaseKey is required" until Preview was checked. Check Vercel → Settings → Env Vars → each row's Environments column when debugging route crashes.
- **Supabase "Confirm email" toggle disables BOTH the gate AND the email send** — not just the gate. Verified via auth logs: no `user_confirmation_requested` events after the toggle flipped. For future verification prompts, use `supabase.auth.resend({ type: 'signup' })` to explicitly trigger.
- **Don't trust memory about what's shipped — verify in code.** Claude claimed photo equipment ID existed when only text-based `identify-vessel` does. `scan-document` accepts photos but only for registration docs, not equipment. Lesson: grep before asserting state.
- **When fixing UX friction, question the requirement before coding around it.** Was about to ship a 100-line PKCE callback route to smooth the post-verify flow. The right fix was a single Supabase dashboard toggle. Saved a day of code + testing + maintenance.
- **"Due Soon" is the correct urgency bucket for a day-zero onboarding task.** Bumping to "Critical" would feel manipulative. The system escalates organically if ignored.
- **Vercel deploy rollbacks are cheap, and rollback instincts from ambiguous symptoms can be wrong.** The error-boundary "white screen" was the iframe quirk, not a code bug. Rollback cost ~10 min; the bigger cost was the confidence hit. Always verify symptom via direct URL before rolling back.

---

## Persona reference

- **Active Cruiser** — validated. 42–65, $150K–$400K HHI, 35–65ft vessel, 20–60 days/year on water, $8K–$40K/yr spend.
- **Liveaboard** — validated. Brand loyalist, word-of-mouth engine.
- **Upgrader** — validated. Tester engaged in beta for weeks; demographics to be filled in post-debrief.
